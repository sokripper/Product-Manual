> 版本：v1.2 ｜ 冻结版本 v1.2 ｜ 冻结 commit b1fbc94db4cef46bd8a4bd920ce5aadc06a4c494

# 服务商索赔单 详细设计

本文覆盖服务商索赔单从新建到结算入口的完整后端行为，字段级、可开发。按 PRD 功能定义清单 F-CLAIM-01~10 一一对应，一个功能点一节。范围表：`claim_order`（主单）、`claim_fee_labor`（工时费明细）、`claim_fee_part`（备件明细）、`claim_fee_other`（其它费用明细）、`claim_audit_record`（审核记录）、`claim_deduct_order`（索赔抵扣单）、`attachment`（附件）。工单库取数一律 Mock、标「待挂载」；向欣旺达索赔单、结算单、质保金账户、供应商索赔、OA 转嫁属于相邻链，本文只写触发点，不写其内部字段。

技术底座：Spring Boot 3 + MyBatis-Plus + MySQL8。主单每次更新都带乐观锁：`WHERE id=? AND status=? AND version=? AND deleted_at IS NULL`，命中 0 行即判为并发冲突或状态已变，抛业务码并整事务回滚。一个操作涉及的多张表在同一数据库事务内提交，任一步失败全部回滚。所有查询附 `deleted_at IS NULL`。

---

## 通用约定

这条链里反复用到、各功能点共享的约定集中放这里，各功能点正文不再重复。

### 谁在操作（RBAC + 数据范围）

- 接口进来先做 RBAC 鉴权（未登录 401，越权 403）+ 数据范围过滤。
- 数据范围：服务商售后专员只看/只动本服务商单；区域负责人只动本区域单；闪欣索赔专员全国；质量部只动【待责任判定】的自采件单。
- 数据范围过滤在 SQL 里拼 `provider_id` / `region` 条件，不靠前端。

### 状态取值（九个码值）

- `claim_order.status` 只允许 `CLAIM_STATUS` 九个码值：`DRAFT / PENDING_REGION / PENDING_HQ / PENDING_LIABILITY / PENDING_SETTLE / REJECTED / DECLINED / SETTLED / VOIDED`。
- 任何写状态的地方都先判「当前状态是否为该操作允许的前置状态」，不是就拦下（见文末迁移总表下方的非法迁移拦截）。
- 术语归一（来自 PRD S7）：「已关闭」=「已拒绝」`DECLINED`（A-15）；「审核驳回」=「已驳回」`REJECTED`（A-4）。

### 单号怎么来

- `claim_no = SPCO-yyyyMMdd-4位日内流水`（PRD S10 编码规则：SPCO-8位年月日-4位流水，全局唯一）。落库前按已确认的 D-编码-D1/D2 方案，从 `doc_no_sequence` 以「前缀+日期」同事务行锁取号，业务 INSERT 成功才占号、回滚退号；`uk_claim_no` 唯一索引兜底。
- 抵扣单号 `deduct_no = DKD-yyyyMMdd-4位流水`，同机制。
- 序列表 DDL 仍待研发组长审定（D-编码-B1）。

### 金额恒等式（落单即算、只读）

- `apply_total_amount = labor_fee_total + part_mgmt_fee + other_fee_total`。
- `labor_fee_total = Σ claim_fee_labor.amount`；`part_mgmt_fee = Σ claim_fee_part.mgmt_fee`；`other_fee_total = Σ claim_fee_other.amount`。
- 备件索赔金额 `claim_fee_part.claim_amount` 恒为 0（PRD S8.2）。

### 系数快照

- 新建时把「发起时点的服务商索赔系数」写进 `claim_order.coefficient_snapshot`，之后系数配置变了也不动历史单（A-2）。
- 备件管理费 `mgmt_fee = claim_fee_part.sale_price（备件销售价，落库）× coefficient_snapshot`（裁决 D-索赔-B11：销售价落库 `sale_price` 使 mgmt_fee 可追溯）。

### 审计列

- 每次写主单都要带 `update_by / updated_at(NOW()) / version=version+1`；新建带 `create_by / created_at`。

### 附件挂哪

- 都进 `attachment` 表，用 `biz_type + biz_id` 定位。
- 索赔单主体附件 `biz_type=CLAIM, biz_id=claim_order.id`；判责录入附件 `biz_type=LIABILITY, biz_id=claim_order.id`。
- 回运单附件按 A-11 也落 `attachment`（判定见 F-CLAIM-02 提交一节，B-3）。

### 全链状态流转总表（谁触发 + 条件 + 写哪些字段 + 拦截）

各功能点「业务流转说明」是本表对应行的展开，本表作为全链一览保留。

| 起态 | 事件（谁） | 终态 | 迁移条件 | 迁移时写的关键字段 |
|---|---|---|---|---|
| —（新建） | 新建（服务商专员） | `DRAFT` | 工单已完工+技术审核已过+保内+条件唯一+工时费>0 | 建 claim_order(status=DRAFT)+三明细+系数快照 |
| `DRAFT` | 提交（服务商专员） | `PENDING_REGION` | 需回运件已发起回运+维修凭证附件在+硬校验 | claim_order.status=PENDING_REGION |
| `DRAFT` | 编辑（服务商专员） | `DRAFT` | 仅改其它费用 | claim_fee_other + 重算 other_fee_total/apply_total_amount |
| `DRAFT` | 作废（服务商专员） | `VOIDED` | 仅草稿 | status=VOIDED；主单/明细/附件保留供全状态查看 |
| `PENDING_REGION` | 区域初审通过（区域负责人） | `PENDING_HQ` | 核查通过+录 claim_target（XW 必录 project_code） | status=PENDING_HQ、claim_target、project_code、audit_record(区域初审/通过) |
| `PENDING_REGION` | 区域初审驳回 | `REJECTED` | 资料不实/汽车电子工时不合理等 | status=REJECTED、audit_record(区域初审/驳回+原因) |
| `PENDING_REGION` | 区域初审拒绝 | `DECLINED` | 判定不予受理 | status=DECLINED、audit_record(区域初审/拒绝+原因) |
| `PENDING_HQ` | 业务审核通过·XW（索赔专员） | `PENDING_SETTLE` | claim_target=XW | status=PENDING_SETTLE、audit_record(业务审核/通过)、**生成向欣旺达单** |
| `PENDING_HQ` | 业务审核通过·SELF（索赔专员） | `PENDING_LIABILITY` | claim_target=SELF | status=PENDING_LIABILITY、audit_record(业务审核/通过) |
| `PENDING_HQ` | 业务审核驳回 | `REJECTED` | 复核不通过 | status=REJECTED、audit_record(业务审核/驳回+原因) |
| `PENDING_HQ` | 业务审核拒绝 | `DECLINED` | 终止 | status=DECLINED、audit_record(业务审核/拒绝+原因) |
| `PENDING_LIABILITY` | 判责录入（质量部） | `PENDING_SETTLE` | 三结果之一+说明 | liability_result/desc/ratio/by/time、status=PENDING_SETTLE、audit_record(audit_stage='LIABILITY' 责任判定/判责结果，裁决 D-索赔-B10)；PROVIDER 建抵扣单+provider_deduct_started=1；SUPPLIER 置 supplier_claim_started=1；INTERNAL 仅记录 |
| `REJECTED` | 编辑（服务商专员） | `REJECTED` | 仅改其它费用 | claim_fee_other + 重算金额（状态不变） |
| `REJECTED` | 重新提交（服务商专员） | `PENDING_REGION` | 同提交校验（从头，A-13） | status=PENDING_REGION |
| `PENDING_SETTLE` | 结算完成（结算链，非本链动作） | `SETTLED` | 由结算链驱动 | status=SETTLED（字段属结算链） |

**非法迁移拦截（统一策略）**：每个写状态的操作，先用乐观锁 SQL 限定「前置状态白名单」——`UPDATE ... WHERE id=? AND status IN (<该操作允许的前置态>) AND version=? AND deleted_at IS NULL`。命中 0 行有两种原因：并发导致 version 变了、或状态已不在白名单（如对已结算单点作废、对草稿单点判责、对已拒绝/已作废终态做任何流转）。一律回滚并返回业务码「当前状态不允许该操作，请刷新」（HTTP 200，`Result.success=false`，PRD S12 乐观锁+二次校验）。终态 `DECLINED / VOIDED / SETTLED` 不作为任何流转事件的前置态。`SETTLED` 由结算链负责，本链不主动写。

---

## F-CLAIM-01 新建索赔

### 功能说明：

- 服务商售后专员关联一张【已完工 + 维修方案已过技术审核 + 保内】的维修工单，自动带出工单基础信息、维修项目、工时标准、备件明细（备件索赔金额=0、备件管理费）、其它费用、维修类型/SN/条码（只读带入，A-10）、主责件；索赔类型由工单业务类型带出只读（A-9）。
- 保存后生成草稿单并带出费用明细（PRD 完成标准）。
- 关键限制：仅保内且已过技术审核的已完工工单可发起（A-8，技术审核前置来源 PRD F-CLAIM-01）；同一工单同时只能有一张有效单（A-15）；工时费合计必须 > 0（A-1）。

### 开发类型：

- 新增节点。

### 节点目录：

- 服务商索赔管理 - 服务商索赔单列表（P01）- 新建索赔单（P02 表单）。

### 业务流转说明：

- 服务商售后专员在「新建索赔单」入口发起，无前置单据状态（是新建）。系统先取工单维度互斥锁、再在事务内校验工单准入与唯一性，通过后同一事务内建主单 `claim_order`（status=DRAFT）+ 三张费用明细 + 系数快照 +（可选）附件 + 一条新建操作留痕，任一步失败整体回滚，不留半张单。落地终态为 `DRAFT`（草稿），不触发任何下游。

### 涉及数据表：

- `claim_order`（索赔主单）：写入主单全部字段（单号/状态/工单带出信息/系数快照/金额汇总/审计列）—— 表结构与状态字典见 `docs/db-schema/`。
- `claim_fee_labor`（工时费明细）：按工单维修项目逐行写入。
- `claim_fee_part`（备件明细）：按工单备件逐行写入。
- `claim_fee_other`（其它费用明细）：写入工单带出的其它费用行。
- `attachment`（附件）：新建时可传可不传，若传按 `biz_type=CLAIM` 逐个写入。
- `claim_audit_record`（审核记录）：写入一条新建操作事件。
- 工单库相关取数表：**只读、待挂载/Mock**，物理列未登记，禁止自造。
- `claim_type_mapping`（工单业务类型→索赔类型映射字典）：只读，用于带出 `claim_type`。
- `claim_coefficient`（服务商索赔系数配置）：只读，用于取系数快照回退值。

### 解决方案：

- **操作按钮**：
    - **新建**（表单，逐字段写规则）：
        - 校验（新建阶段从宽，只拦硬错）：
            1. 关联工单必须是「本服务商 + 状态=已完工 + 维修方案已过技术审核 + 保内」的维修工单（A-8；技术审核前置来源 PRD F-CLAIM-01）。工单状态/保内标识/服务商归属/维修方案技术审核结果都从工单库取——**待挂载/Mock**，本期用 Mock 数据源判断（技术审核结果本期由工单侧 Mock 保证，真库接上后回填判据）。
            2. 同一工单已存在一张状态非 `DECLINED/VOIDED` 的索赔单则拦下（A-15：只有已拒绝/已作废才允许重新发起；`REJECTED` 仍是可编辑重提的同一张有效单，不能另建）。为避免「先查后插」并发穿透，新建必须使用同一 DB 连接先取工单维度互斥锁 `SELECT GET_LOCK(CONCAT('claim-wo-',MD5(?)),5)`；取锁失败/超时直接返回「该工单正在发起索赔，请稍后重试」。取锁后在事务内再查 `claim_order WHERE work_order_id=? AND status NOT IN (DECLINED,VOIDED) AND deleted_at IS NULL`，通过后才插入；提交/回滚后在 `finally` 中用同连接 `RELEASE_LOCK`。该锁是连接级而非事务级，不得在取锁与放锁之间归还连接池。
            3. 工时费合计必须 > 0（A-1，S8.2「工时费>0 必有」）；其它费用合计 ≥ 0。
            4. 草稿阶段允许必填项暂缺（附件、claim_target 等留白），提交时再严格校验。
        - 操作按钮：
            - 保存：写入以下数据（同一事务）：
                - 写入 `claim_order`（插一行）：
                    - `id` = 雪花
                    - `claim_no` = 生成的 SPCO 号（见「编码规则」）
                    - `status` = 'DRAFT'
                    - `currency` = 'CNY'
                    - `work_order_id / work_order_no / work_order_biz_type / provider_id / provider_code / provider_name / region` = 工单库带出（**待挂载/Mock**）
                    - `claim_type` = 按 `work_order_biz_type` 查 `claim_type_mapping` 映射得到，只读带入（A-9）；映射查不到时留空并报「工单业务类型未配置映射」拦截（不可静默）
                    - `coefficient_snapshot` = 发起时点服务商系数：优先取工单库系数（待挂载），无则查本模块 `claim_coefficient` 当前生效值。「当前生效值」选取谓词：`enabled=1 AND deleted_at IS NULL AND COALESCE(effective_from,'1000-01-01') <= 发起日`，先按 `provider_id=:providerId` 命中、无命中行时回退 `provider_id IS NULL`（全局默认行）；同一命中范围内多行时按 `effective_from DESC, created_at DESC` 取最新一行（与白名单 effective_from 同口径）
                    - `labor_fee_total / part_mgmt_fee / other_fee_total` = 由三张明细汇总回填
                    - `apply_total_amount` = 三者之和
                    - `main_part_id / main_part_name` = 工单主责件带出（**待挂载/Mock**），可为空（区域初审前允许）
                    - `repair_type / sub_repair_type / serial_no / vin / province_city` = 工单库带出的维修类型/二级类型/序列号/整车 VIN/所属省市（只读带入 A-10，**待挂载/Mock**；裁决 D-索赔-B9 已加列，支撑 A-14 汽车电子把关与 A-39 场景条件必填的分类判据），无值可空
                    - `claim_target` = NULL、`project_code` = NULL（区域初审才录）
                    - `liability_*` = NULL
                    - `supplier_claim_started` = 0、`provider_deduct_started` = 0
                    - `create_by` = 当前用户、`created_at` = NOW()、`version` = 0
                - 写入 `claim_fee_labor`（按工单维修项目逐行插）：
                    - `claim_id` = 主单 id
                    - `project_name / labor_hours / labor_price` = 工单带出（待挂载）
                    - `amount` = `labor_hours × labor_price`
                - 写入 `claim_fee_part`（按工单备件逐行插）：
                    - `claim_id` = 主单 id
                    - `part_name / part_attr / battery_sn / old_part_code` = 工单带出（待挂载）
                    - `sale_price` = 工单备件销售价（**待挂载/Mock 带出**，裁决 D-索赔-B11 已加列落库，保金额可追溯）
                    - `claim_amount` = 0.00（恒 0）
                    - `mgmt_fee` = `sale_price × coefficient_snapshot`（销售价落库于 `sale_price`，派生可复核，裁决 D-索赔-B11）
                - 写入 `claim_fee_other`（插工单带出的其它费用行）：
                    - `claim_id` = 主单 id
                    - `fee_type / amount / remark` = 工单带出
                    - `editable` = 物流费等可人工改的置 1，其余置 0（A-7）
                - 写入 `attachment`（此刻可传可不传；若传逐个插）：`biz_type=CLAIM`、`biz_id`=新单 id
                - 写入 `claim_audit_record`（新建操作事件）：`biz_type='CLAIM'`、`biz_id`=新单 id、`audit_stage='OPERATION'`、`audit_result='CREATE'`、`auditor/audit_time`=操作人/时间、`remark='新建草稿'`
            - 取消：不保存，返回上一页。

### 编码规则：

- `claim_no` = `SPCO-yyyyMMdd-4位日内流水`，例 `SPCO-20260712-0007`（依据：PRD S10 编码规则；D-编码-D1/D2 已确认，序列表 DDL 待 D-编码-B1）。同事务行锁取号、INSERT 成功才占号、回滚退号，`uk_claim_no` 兜底。

### 外部接口：

- 工单库取数：对接系统为维修工单管理模块；触发时机为进入新建表单带出工单基础信息/维修项目/工时/备件/主责件/系数/技术审核结果/VIN·SN·条码。本期 **Mock/待挂载**，物理列未登记，真库到位走变更登记回填（A-21，见「留白与取数边界」）。失败处理：Mock 源缺关键判据（技术审核结果等）时按校验规则拦截，不静默放行。

### 导入：

- 无（新建单条表单录入；批量导入场景见 F-CLAIM-09 涉及的汽车电子旧件条码 Excel，属相邻能力，不在本节）。

### 防重复与并发：

- 工单维度互斥锁 `GET_LOCK('claim-wo-'+MD5(work_order_id))` 连接级串行化，防「先查后插」并发穿透造成一工单两张有效单；取锁失败即返回重试。
- 主单+三张明细+附件+操作事件同一事务，任一插入失败整体回滚。
- `uk_claim_no` 唯一索引兜底单号并发重复。

---

## F-CLAIM-02 提交索赔

### 功能说明：

- 服务商售后专员把草稿或已驳回单提交，进入区域初审（PRD 完成标准：状态→待区域初审）。
- 提交时维修凭证附件必填（原始 PRD S4.1.1，硬校验），旧件资料附件选填；校验备件是否需回运，需回运的必须先发起回运（在途即可提交，A-11）。
- 也用于「已驳回」单编辑后重新提交（A-13，从头走区域初审）。

### 开发类型：

- 新增节点。

### 节点目录：

- 服务商索赔管理 - 服务商索赔单列表（P01）- 详情/编辑页（P02）- 提交。

### 业务流转说明：

- 服务商售后专员对 `DRAFT` 或 `REJECTED`（已驳回可编辑后重提，A-13）单点提交。系统重走一遍新建硬校验并叠加回运/维修凭证/条件必填校验，通过后单表更新 `claim_order.status='PENDING_REGION'` 并写一条提交操作留痕，同一事务、走乐观锁。从 `REJECTED` 重提同样回到 `PENDING_REGION`（从头走）。落地终态 `PENDING_REGION`（待区域初审），不触发下游（等区域初审）。

### 涉及数据表：

- `claim_order`（索赔主单）：更新 `status` 与审计列，其余业务字段不动；`repair_type/sub_repair_type`（场景触发）、`vin`（整车条件必填）只读用于 A-39 硬校验（裁决 D-索赔-B9）。
- `claim_audit_record`（审核记录）：写一条提交操作事件。
- `claim_fee_part`（备件明细）：只读，用于回运与条件必填校验（`battery_sn`/`old_part_code`）。
- `attachment`（附件）：只读，用于维修凭证必填与回运单附件判定。

### 解决方案：

- **操作按钮**：
    - **提交**（只有 `DRAFT` 或 `REJECTED` 状态的可以提交）：
        - 前置校验：
            1. 走一遍新建的硬校验（工单仍为已完工、维修方案已过技术审核、保内、本服务商、条件唯一、工时费>0；技术审核前置来源 PRD F-CLAIM-01，本期由工单侧 Mock 保证）。
            2. **旧件回运**（A-11）：逐条看 `claim_fee_part` 是否「需回运」；需回运的必须「已发起回运」才放行，否则拦【存在需回运备件未发起回运，不能提交】。
                - 「是否需回运」判定：读 `claim_fee_part.part_attr` 是否含「需回运」标识（**B-2**：当前无结构化布尔位，暂按属性文本判定，建议工单取数补布尔字段）。
                - 「已发起回运」判定：本模块未登记回运单实体/回运状态；按 A-11「售后仓点确认+回运单附件即视为已发起」，暂以「该单存在回运单附件」作为已发起证据（**B-3**：回运单实体/在途状态待挂载，附件判定为临时口径）。
            3. **维修凭证附件必填（有据·硬校验）**：提交时必须至少 1 张维修凭证类附件（维修照片/检测报告等），无维修凭证不可提交（原始 PRD S4.1.1 服务商索赔单字段表明文「维修凭证附件…提交时必填」）。旧件资料附件仍为选填（原始 S4.1.1 标非必填），不随此项一起强制。
            4. **A-39 条件必填 + 位长校验（硬校验，裁决 D-索赔-B9）**（来源 PRD S8.6/A-39）：分类列 `claim_order.repair_type/sub_repair_type` 已按 B9 落库、场景触发有数据支撑，本期即做**硬校验**（内部场景豁免）：
                - `claim_fee_part.battery_sn`（电池包 SN）：**电池包维修场景**必填、须为 **24 位**，不满足 → 拦。
                - `claim_fee_part.old_part_code`（汽车电子旧件条码，Excel 批量场景）：**汽车电子场景**必填，缺失 → 拦。
                - `claim_order.vin`（整车 VIN）：**整车场景**必填、须为 **17 位**（VIN 列已按 B9 补入本链 `claim_order`，不再豁免本链校验；内部场景仍豁免，A-39）。
                - **场景触发口径**：是否电池包/汽车电子/整车由 `claim_order.repair_type/sub_repair_type`（B9 已落库，工单库 Mock/待挂载带出值）判定；分类命中对应场景即按上述硬校验拦截，缺失/位长不符统一拦【条件必填/位长校验不通过】。
        - 若选择「是」，则更新以下数据（同一事务，乐观锁）：
            - `claim_order`：`status` = 'PENDING_REGION'；审计列刷新；其余业务字段不动
            - `claim_audit_record`（插一条操作事件）：`biz_type='CLAIM'`、`audit_stage='OPERATION'`、`audit_result='SUBMIT'`（已驳回重提为 `RESUBMIT`）、`auditor/audit_time`=操作人/时间、`remark`=操作摘要
        - 若选择「否」，则返回，不做处理。

### 编码规则：

- 无新增（沿用 F-CLAIM-01 的 `claim_no`）。

### 外部接口：

- 无（回运单实体/在途状态待挂载，本期以回运单附件为临时判定口径，B-3；不对接外部）。

### 导入：

- 无。

### 防重复与并发：

- 单表更新，乐观锁未命中（并发/状态已变）即回滚报冲突。
- 前置状态白名单 `status IN (DRAFT, REJECTED)`，防重复点击/并发双提交。

---

## F-CLAIM-03 编辑

### 功能说明：

- 仅草稿/已驳回可编辑；工时费、备件管理费不可改，其它费用明细（物流费等）可人工编辑，合计随明细计算（A-7）。
- 保存生效、可重新提交（PRD 完成标准）。

### 开发类型：

- 新增节点。

### 节点目录：

- 服务商索赔管理 - 服务商索赔单列表（P01）- 详情/编辑页（P02）- 编辑。

### 业务流转说明：

- 服务商售后专员对 `DRAFT` 或 `REJECTED` 单编辑，只动其它费用明细。系统同一事务内 upsert/删除 `claim_fee_other` 对应行，重算主单 `other_fee_total` 与 `apply_total_amount` 回写，并写一条编辑操作留痕。状态不变（草稿仍草稿、已驳回仍已驳回），改完可再走提交。不触发下游。

### 涉及数据表：

- `claim_fee_other`（其它费用明细）：按编辑结果 upsert/删除对应行。
- `claim_order`（索赔主单）：重算金额汇总 + 审计列，`status` 不变。
- `claim_audit_record`（审核记录）：写一条编辑操作事件。

### 解决方案：

- **操作按钮**：
    - **编辑保存**（只有 `DRAFT` 或 `REJECTED` 状态可编辑，其它状态一律不可编辑）：
        - 能改什么：只能动「其它费用明细」（物流费等，A-7）。工时费明细、备件明细、备件管理费一律**不可改**（工时费/备件管理费按 S8.2 固定，来源工单+系数）。允许对 `claim_fee_other` 中 `editable=1` 的行做改金额/备注/增行/删行；`editable=0` 的行禁止改删（D-索赔-B8 已确认）。
        - 保存，则更新以下数据（同一事务）：
            - `claim_fee_other`：按编辑结果 upsert/删除对应行
            - `claim_order`：重算 `other_fee_total` = `Σ claim_fee_other.amount`，随后 `apply_total_amount` = `labor_fee_total + part_mgmt_fee + other_fee_total`（labor 和 part 两项不变）；审计列刷新；`status` 不变
            - `claim_audit_record`（插一条）：`audit_stage='OPERATION'`、`audit_result='EDIT'`、`auditor/audit_time`=操作人/时间、`remark`=记录本次其它费用增删改摘要
        - 取消：不保存，返回。

### 编码规则：

- 无。

### 外部接口：

- 无。

### 导入：

- 无。

### 防重复与并发：

- 明细与主单汇总在同一事务，金额必须一致回写，失败全回滚。
- 主单更新走乐观锁；前置状态白名单 `status IN (DRAFT, REJECTED)`。

---

## F-CLAIM-04 作废

### 功能说明：

- 仅草稿可作废（PRD F-CLAIM-04）；作废后状态→已作废（终态）。
- 已作废是可查看的业务终态，不以技术软删除隐藏；作废后该工单可重新发起新单（A-15）。

### 开发类型：

- 新增节点。

### 节点目录：

- 服务商索赔管理 - 服务商索赔单列表（P01）- 作废。

### 业务流转说明：

- 服务商售后专员对**仅** `DRAFT` 单点作废，非草稿点作废一律拦下。系统同一事务把主单 `status='VOIDED'`（`deleted_at` 保持 NULL）、刷新审计列，并写一条作废操作留痕；明细行与附件保留不删。落地终态 `VOIDED`，不触发下游。同一工单重新发起由条件唯一规则排除 `VOIDED` 实现，不依赖软删除。

### 涉及数据表：

- `claim_order`（索赔主单）：更新 `status='VOIDED'` + 审计列，`deleted_at` 保持 NULL。
- `claim_fee_*`（三张明细）、`attachment`（附件）：保留不删，确保 F-CLAIM-05 全状态查看、附件和操作留痕仍可访问。
- `claim_audit_record`（审核记录）：写一条作废操作事件。

### 解决方案：

- **操作按钮**：
    - **作废**（前置状态**仅** `DRAFT`，非草稿一律拦下）：
        - 点击后弹出确认框，确认作废原因。
        - 若选择「是」，则更新以下数据（同一事务）：
            - `claim_order`：`status` = 'VOIDED'；`deleted_at` 保持 NULL；审计列刷新
            - 明细行 `claim_fee_*` 与附件保留不删
            - `claim_audit_record`（与头表状态更新同事务写）：`audit_stage='OPERATION'`、`audit_result='VOID'`、`auditor/audit_time`=操作人/时间、`remark`=作废原因或「作废」
        - 若选择「否」，则返回，不做处理。

### 编码规则：

- 无。

### 外部接口：

- 无。

### 导入：

- 无。

### 防重复与并发：

- 主单更新走乐观锁；前置状态白名单**仅** `DRAFT`，对非草稿/已作废终态点作废命中 0 行即回滚报「当前状态不允许该操作」。

---

## F-CLAIM-05 查看

### 功能说明：

- 全状态可查看（PRD F-CLAIM-05）；含全流程审核记录、附件、操作日志、责任判定。
- 只读，不写库；操作日志不再用「最后修改人/时间」代替历史，新建、编辑、提交、重提、作废、判责均一次动作一条事件。

### 开发类型：

- 新增节点。

### 节点目录：

- 服务商索赔管理 - 服务商索赔单列表（P01）- 详情页（P02）- 查看。

### 业务流转说明：

- 全部相关角色（按数据范围）对任意状态单点查看。系统只读聚合主单 + 三张费用明细 + 审核记录（含操作事件） + 附件（按 biz_type 分组） + 责任判定字段 + 抵扣单（如有），无状态变更、不写库。

### 涉及数据表（均只读）：

- `claim_order`（索赔主单）：主单字段。
- `claim_fee_labor / claim_fee_part / claim_fee_other`（三张费用明细）。
- `claim_audit_record`（审核记录）：审核记录与 `audit_stage='OPERATION'` 的操作事件，按 `audit_time,id` 升序。
- `attachment`（附件）：按 biz_type 分组（维修凭证/旧件/回运单/判责）。
- `claim_deduct_order`（索赔抵扣单）：如有则展示。

### 解决方案：

- **查询结果**（详情只读聚合）：
    - **查询数据范围：按当前角色数据范围过滤（服务商本店/区域本区/索赔专员全国/质量部本人待责任判定件）+ `deleted_at IS NULL`（主单软删过滤，作废单不软删仍可见）**
    - 主单信息：取 `claim_order` 全部业务字段（单号/状态/工单信息/金额/系数/主责件/分类信息 repair_type/sub_repair_type/serial_no/vin/province_city（裁决 D-索赔-B9）/claim_target/project_code/责任判定字段）
    - 工时费明细：取 `claim_fee_labor`（project_name/labor_hours/labor_price/amount）
    - 备件明细：取 `claim_fee_part`（part_name/part_attr/sale_price（裁决 D-索赔-B11）/battery_sn/old_part_code/claim_amount=0/mgmt_fee）
    - 其它费用明细：取 `claim_fee_other`（fee_type/amount/remark/editable）
    - 审核/操作日志：取 `claim_audit_record`（审核记录与操作事件，按 `audit_time,id` 升序），一动作一条事件
    - 附件：取 `attachment`，按 biz_type 分组（维修凭证/旧件/回运单/判责）
    - 责任判定：取 `claim_order.liability_result/liability_desc/liability_ratio/liability_by/liability_time`
    - 抵扣单（如有）：取 `claim_deduct_order`
- **操作按钮**：返回列表（无写库动作）。

### 编码规则：

- 无。

### 外部接口：

- 无。

### 导入：

- 无。

### 防重复与并发：

- 只读，无并发写；数据范围过滤在 SQL，不靠前端。

---

## F-CLAIM-06 区域初审

### 功能说明：

- 区域负责人核查工单真实性/资料完整性；录入索赔对象（是否欣旺达）、欣旺达时必填项目编码；含 SN 自动预判责任（可覆盖，非终判，A-26）；汽车电子自由填报工时在此把关可驳回（A-14）。
- 通过→待总部审核（PRD 完成标准）。

### 开发类型：

- 新增节点。

### 节点目录：

- 服务商索赔管理 - 服务商索赔单列表（P01）- 审核弹窗 - 区域初审。

### 业务流转说明：

- 区域负责人（只限本区域）对 `PENDING_REGION` 单做区域初审，录 `claim_target`（XW/SELF）、XW 时录 `project_code`，可含 SN 预判倾向。三种结果分别流转：通过→`PENDING_HQ`；驳回→`REJECTED`（退回服务商，可编辑重提回 `PENDING_REGION`）；拒绝→`DECLINED`（终态不可再动）。每种结果同一事务更新主单 + 写一条审核记录，走乐观锁。不触发下游（等业务审核）。

### 涉及数据表：

- `claim_order`（索赔主单）：更新 `status`、`claim_target`、`project_code`、审计列。
- `claim_audit_record`（审核记录）：写一条区域初审记录（SN 预判倾向本期写入其 `remark`）。
- `claim_fee_part`（备件明细）：只读，取 `battery_sn` 做预判。

### 解决方案：

- **操作按钮**：
    - **区域初审**（前置状态 `PENDING_REGION`，只限本区域）：
        - 要核查/要录的：
            1. 核查工单真实性、资料完整性（人工，不入库字段，通过与否体现在结果里）。
            2. 录 `claim_target`（`XW`=欣旺达件 / `SELF`=自采件，即主责件归属，A-6）。
            3. `claim_target=XW` 时 `project_code`（项目编码）必填；`=SELF` 时 project_code 置 NULL。
            4. **电池包 SN 自动预判责任**（A-26，可覆盖、非终判）：对含 `battery_sn` 的备件，系统按 SN 预判一个责任倾向给质量部参考。预判规则来源为 QMS，本期 **Mock/待挂载**；预判值无专用登记字段，本期写入本次 `claim_audit_record.remark`，不新增专列；QMS 正式契约到位后再补预判字段与口径（D-索赔-B4 已确认）。此预判不写 `claim_order.liability_result`（终判专用）。
            5. **汽车电子自由填报工时把关**（A-14）：一级分类=汽车电子的单，工时为自由填报，区域负责人在此判合理性，不合理可**驳回**（一级分类取 `claim_order.repair_type`，已按裁决 D-索赔-B9 落库，工单库 Mock/待挂载带出）。
        - 结果一 · **通过**，则更新（同一事务，乐观锁）：
            - `claim_order`：`status`='PENDING_HQ'；`claim_target`=录入值；`project_code`=录入值(XW) 或 NULL(SELF)；审计列刷新
            - `claim_audit_record`（插一行）：`biz_type`='CLAIM'、`biz_id`=主单 id、`audit_stage`='区域初审'、`auditor`=当前用户、`audit_time`=NOW()、`audit_result`='通过'、`remark`=核查意见（含本期 Mock 的 SN 预判倾向，D-索赔-B4）、`approved_amount`=NULL、`attachment_ids`=如有
        - 结果二 · **驳回**（退回服务商，A-13），则更新：
            - `claim_order`：`status`='REJECTED'；`claim_target/project_code` 若已录可保留也可留空（不影响，重提会从头再录）；审计列刷新
            - `claim_audit_record`：同上但 `audit_result`='驳回'、`remark`=驳回原因（必填）
        - 结果三 · **拒绝**（终止，A-15 已关闭=已拒绝），则更新：
            - `claim_order`：`status`='DECLINED'；审计列刷新
            - `claim_audit_record`：`audit_result`='拒绝'、`remark`=拒绝原因（必填）

### 编码规则：

- 无。

### 外部接口：

- QMS 判责预判：对接系统为 QMS 判责系统；触发时机为区域初审对含 `battery_sn` 备件做 SN 预判。本期 **Mock/待挂载**（A-20），预判值暂写 `claim_audit_record.remark`；正式契约到位后补预判字段与口径（D-索赔-B4）。失败处理：Mock 无预判值时留空，不阻断初审。

### 导入：

- 无。

### 防重复与并发：

- 主单状态 + 审核记录同一事务，乐观锁保并发；前置状态白名单 `PENDING_REGION`。
- 已驳回可编辑重提（回 `PENDING_REGION`）；已拒绝终态不可再动。

---

## F-CLAIM-07 业务审核

### 功能说明：

- 闪欣索赔专员复核；通过后按**主责件归属唯一决定流向**（A-6）分支：欣旺达件→逐单自动生成向欣旺达索赔单（A-36）+进待结算；外采件→进【待责任判定】（A-12）。
- 通过→待责任判定/待结算（PRD 完成标准）。

### 开发类型：

- 新增节点。

### 节点目录：

- 服务商索赔管理 - 服务商索赔单列表（P01）- 审核弹窗 - 业务审核。

### 业务流转说明：

- 闪欣索赔专员（全国）对 `PENDING_HQ` 单做业务审核，通过后按 `claim_order.claim_target`（区域初审已录）唯一分流：XW→`PENDING_SETTLE` 并在**同一事务**逐单自动生成一张向欣旺达索赔单草稿；SELF→`PENDING_LIABILITY`（等质量部判责），无自动生成。驳回→`REJECTED`（可重提，回 `PENDING_REGION` 从头）；拒绝→`DECLINED`（终态）。每种结果同一事务更新主单 + 写审核记录，走乐观锁。

### 涉及数据表：

- `claim_order`（索赔主单）：更新 `status`、审计列。
- `claim_audit_record`（审核记录）：写一条业务审核记录。
- `xw_claim_order` / `xw_claim_detail`（向欣旺达索赔单，属相邻链）：XW 通过时触发生成草稿——本文只登记触发与关联键，不写其内部字段。

### 解决方案：

- **操作按钮**：
    - **业务审核**（前置状态 `PENDING_HQ`，闪欣索赔专员全国）：核心：通过后按主责件归属唯一分流（A-6/A-12/A-36），看 `claim_order.claim_target`。
        - 结果一 · **通过 · claim_target=XW（欣旺达件）**，则更新（同一事务，乐观锁）：
            - `claim_order`：`status`='PENDING_SETTLE'（进待结算）；审计列刷新
            - `claim_audit_record`（插）：`audit_stage`='业务审核'、`audit_result`='通过'、`auditor/audit_time/remark`
            - **触发下游**：逐单自动生成一张向欣旺达索赔单（A-36，`xw_claim_order` + `xw_claim_detail` 草稿，金额=本单对应金额不乘系数 A-37）。该生成动作与主单状态更新放在**同一事务**保证「审核通过即有下游单」；`xw_claim_order` 的字段填充规则属向欣旺达链 LLD，本文只登记触发与关联键 `provider_claim_id=claim_order.id / provider_claim_no=claim_no`。`claim_order` 侧无回写字段（无 xw 关联列）
        - 结果二 · **通过 · claim_target=SELF（自采/外采件）**，则更新：
            - `claim_order`：`status`='PENDING_LIABILITY'（进待责任判定，A-12）；审计列刷新
            - `claim_audit_record`（插）：`audit_stage`='业务审核'、`audit_result`='通过'
            - **触发下游**：无自动生成，等质量部判责
        - 结果三 · **驳回 / 拒绝**，则更新：同区域初审的驳回/拒绝语义——`status`='REJECTED'（可重提，回 PENDING_REGION 从头）或 `status`='DECLINED'（终态）；`claim_audit_record` 记 `audit_stage`='业务审核'、`audit_result`='驳回'/'拒绝'、`remark`=原因（必填）

### 编码规则：

- 无（向欣旺达单号 SXCO 属相邻链，不在本链生成）。

### 外部接口：

- 无外部系统对接（向欣旺达为本系统内建角色 A-22）；下游向欣旺达单生成属相邻链模块内调用，见「触发下游」。

### 导入：

- 无。

### 防重复与并发：

- 主单 + 审核记录 +（XW 时）向欣旺达单生成全在一个事务；向欣旺达单生成失败则整个审核回滚，主单不置通过（避免「通过了但没生成下游单」）。
- 前置状态白名单 `PENDING_HQ`；核心动作叠加按 `claim_order.id` 分布式锁串行化，避免同单并发双审（PRD S12/S15）。
- 向欣旺达单生成防重复生成：以「一张服务商单仅可纳入一张闪欣单」约束兜底（下游 `xw_claim_detail.uk_provider_claim`）。

---

## F-CLAIM-08 责任判定

### 功能说明：

- 质量部对外采件判定；结果三项：供应商责任/服务商责任/内部责任（A-27），录责任说明+承担比例；服务商责任→自动生成索赔抵扣单；供应商责任→触发供应商索赔流程；内部责任→记录比例/费用内部转嫁。
- 判责完成→待结算（PRD 完成标准，S6.1）。

### 开发类型：

- 新增节点。

### 节点目录：

- 服务商索赔管理 - 服务商索赔单列表（P01）- 判责录入弹窗 - 责任判定。

### 业务流转说明：

- 质量部责任判定专员（只限【待责任判定】的自采件单）对 `PENDING_LIABILITY` 单录判责结果。系统同一事务写公共判责字段 + 判责操作留痕 +（如有）判责附件，主单统一进 `PENDING_SETTLE`；按结果再做：PROVIDER 生成索赔抵扣单并置 `provider_deduct_started=1`；SUPPLIER 仅置 `supplier_claim_started=1`（供应商索赔子流程 V2 范围，本期不落单）；INTERNAL 仅记录比例/说明（内部转嫁交 OA，本期待挂载）。走乐观锁。

### 涉及数据表：

- `claim_order`（索赔主单）：写判责字段（liability_result/desc/ratio/by/time）、status、启动标志、审计列。
- `claim_audit_record`（审核记录）：写一条判责留痕。
- `attachment`（附件）：判责附件如有则按 `biz_type=LIABILITY` 逐条写入。
- `claim_deduct_order`（索赔抵扣单）：PROVIDER 结果时插一行。

### 解决方案：

- **操作按钮**：
    - **判责录入**（前置状态 `PENDING_LIABILITY`，只限质量部对自采件单）：
        - 要录的：责任判定结果 `liability_result`（三选一：`SUPPLIER` 供应商 / `PROVIDER` 服务商 / `INTERNAL` 内部，A-27）、责任说明 `liability_desc`（必填，≤200）、承担比例 `liability_ratio`（%，`DECIMAL(5,2)`；校验 `0 < ratio ≤ 100`，不允许 0；`PROVIDER` 责任必填，其余按需，D-索赔-B7 已确认）。可传判责附件（`attachment`，`biz_type=LIABILITY`）。
        - 提交则更新以下数据（三种结果都写的**公共写入**，同一事务，乐观锁）：
            - `claim_order`：`liability_result`=录入值、`liability_desc`=录入值、`liability_ratio`=录入值、`liability_by`=当前用户、`liability_time`=NOW()、`status`='PENDING_SETTLE'；审计列刷新
            - **判责操作留痕**（F-CLAIM-08 判责是一个独立用户动作，须与新建/提交/审核等动作一致进审核时间线，F-CLAIM-05「全流程留痕/操作日志」）：`claim_audit_record` 插一条：`biz_type`='CLAIM'、`biz_id`=主单 id、`audit_stage`=**'LIABILITY'（责任判定阶段码，裁决 D-索赔-B10 已决增该码并落进 schema/DDL）**、`audit_result`=判责结果 `SUPPLIER/PROVIDER/INTERNAL`（该三值已在 `audit_result` 列注释登记）、`auditor`=判定人、`audit_time`=NOW()、`remark`=责任说明（`liability_desc`）、`attachment_ids`=判责附件 id（如有）
            - 判责附件若有 → `attachment` 逐条插（`biz_type=LIABILITY, biz_id=claim_order.id`）
        - **按结果分别再做**：
            - **PROVIDER（服务商责任）→ 自动生成索赔抵扣单**：
                - `claim_order`：`provider_deduct_started`=1
                - `claim_deduct_order` 插一行：`id`=雪花；`deduct_no`=生成 DKD 号；`claim_id`=主单 id；`claim_no`=主单号；`provider_id`=主单 provider_id；`work_order_no`=主单 work_order_no；`liability_result`='PROVIDER'；`liability_ratio`=录入比例；`claim_liable_amount = claim_order.apply_total_amount`（抵扣针对服务商全额应付，D-索赔-D1 已确认）；`deduct_amount`=`claim_liable_amount × liability_ratio / 100`（A-30）；`settle_period_belong`=该索赔单 `claim_audit_record` 中「业务审核通过」的 `DATE(audit_time)`（A-30，按闪欣审批通过时间归入同周期，不得写 NULL）；`status`='UNSETTLED'
                - 抵扣单被结算链汇总时冲减服务商应付（本链只负责生成，不管结算）
            - **SUPPLIER（供应商责任）→ 触发供应商索赔流程**：
                - `claim_order`：`supplier_claim_started`=1
                - 供应商索赔子流程为 V2 范围（PRD S5「9月项」）；本期只置 `supplier_claim_started=1`、标记**待挂载**，不生成实际单据或占位单（D-索赔-B5 已确认）。备件费走供应商索赔（S8.3），本期不落单
            - **INTERNAL（内部责任）→ 记录比例，内部转嫁**：
                - 仅记录 `liability_result/liability_ratio/liability_desc`（公共写入已含）；费用内部转嫁交 OA，本期**待挂载**，不生成转嫁单（**B-6**，A-19）

### 编码规则：

- `deduct_no` = `DKD-yyyyMMdd-4位流水`（PROVIDER 生成抵扣单时，从 `doc_no_sequence` 同事务行锁取号，`deduct_no` 唯一兜底；D-编码-D1/D2）。

### 外部接口：

- QMS 判责系统：对接系统 QMS；触发时机为质量部判责录入（本期人工录入，A-20 Mock/待挂载）。契约未提供，V2 走 EXT-01 真实对接。失败处理：本期人工录入，无自动回传。
- 供应商索赔流程：SUPPLIER 结果触发，本期**待挂载**仅置 `supplier_claim_started=1`，不生成单据（D-索赔-B5）。
- OA 内部转嫁：INTERNAL 结果触发，本期**待挂载**仅记录比例/说明，不生成转嫁单（B-6/A-19，EXT-04）。

### 导入：

- 无。

### 防重复与并发：

- 主单判责字段 + 抵扣单 + 附件同一事务；抵扣单生成失败则判责整体回滚，主单不置待结算（避免「判了服务商责任却没抵扣单」）。
- 前置状态白名单 `PENDING_LIABILITY`，且只 `PENDING_LIABILITY→PENDING_SETTLE` 单向迁移，天然防重复生成抵扣单；抵扣单以 `deduct_no` 唯一兜底。
- 核心动作叠加按 `claim_order.id` 分布式锁串行化。

---

## F-CLAIM-09 搜索/导出

### 功能说明：

- 多维筛选（编号/工单/类型/服务商/区域/时间/状态）；按筛选导出（PRD F-CLAIM-09）。
- 本功能点本期无独立详设定稿，**解决方案沿用通用列表查询/导出规格（待 E-通用-B2 定稿）**；本节写清从 PRD/原型能拿到的查询条件/结果列/权限范围，不编造未定细节，标待确认指针。

### 开发类型：

- 新增节点。

### 节点目录：

- 服务商索赔管理 - 服务商索赔单列表（P01）- 查询/导出。

### 业务流转说明：

- 各相关角色在列表页（P01）填写多维筛选条件查询本人数据范围内的索赔单，命中结果分页展示、本页合计申请总金额；点导出按当前筛选条件导出明细（大数据量异步导出+进度，PRD S12）。只读、不写库。**导出字段清单与异步/同步策略、分片阈值等通用规格待 E-通用-B2 定稿**。

### 涉及数据表（均只读）：

- `claim_order`（索赔主单）：列表主表，按数据范围 + 筛选条件过滤。
- 关联取 `provider`（服务商名/区域）、`claim_type_mapping`（索赔类型显示）等维度，具体关联路径随通用列表规格定稿（E-通用-B2）。

### 解决方案：

- **查询条件**（来自原型 P01 与 PRD F-CLAIM-09）：
    - 索赔单编号：文本输入框，默认空，来源 `claim_order.claim_no`
    - 工单编号：文本输入框，默认空，来源 `claim_order.work_order_no`
    - 索赔类型：下拉（全部类型），默认全部，来源 `claim_order.claim_type`
    - 索赔单状态：下拉（全部状态），默认全部，来源 `claim_order.status`（九态）
    - 服务商：下拉（全部服务商），默认全部，来源 `claim_order.provider_id/provider_name`
    - 所属区域：下拉（全部区域），默认全部，来源 `claim_order.region`
    - 创建时间：日期区间（开始~结束），默认空，来源 `claim_order.created_at`
- **查询结果**：
    - **查询数据范围：按当前角色数据范围过滤（服务商本店 provider_id / 区域本区 region / 索赔专员全国 / 质量部本人待责任判定件）+ `deleted_at IS NULL` + 上述筛选条件**
    - 索赔单编号：取 `claim_order.claim_no`
    - 工单编号：取 `claim_order.work_order_no`
    - 服务商：取 `claim_order.provider_name`
    - 所属区域：取 `claim_order.region`
    - 索赔类型：取 `claim_order.claim_type`
    - 申请总金额：取 `claim_order.apply_total_amount`
    - 状态：取 `claim_order.status`
    - 创建人：取 `claim_order.create_by`
    - 创建时间：取 `claim_order.created_at`
    - 操作列：按状态+角色渲染（草稿→编辑/提交/作废；待区域初审/待总部审核→查看/审核；其它→查看/编辑），本页合计申请总金额
- **操作按钮**：
    - **查询 / 重置**：按条件查询 / 清空条件
    - **导出**：
        - 数据范围限制：按当前查询条件 + 角色数据范围，只导本人可见数据
        - 导出字段：与查询结果列一致（编号/工单/服务商/区域/类型/申请总金额/状态/创建人/创建时间）
        - **导出字段完整清单、异步导出+进度、分片阈值等通用规格待 E-通用-B2 定稿**（PRD S12「大数据异步导出+进度」）

### 编码规则：

- 无。

### 外部接口：

- 无。

### 导入：

- 无（导出方向；汽车电子旧件条码 Excel 批量属备件录入侧能力，触发条件必填校验见 F-CLAIM-02 A-39）。

### 防重复与并发：

- 只读，无并发写；导出大数据量走异步任务（通用规格待 E-通用-B2）。

---

## F-CLAIM-10 批量审核

### 功能说明：

- 区域/索赔专员勾选批量审核（部分成功部分失败）（PRD F-CLAIM-10）；返回成功/失败明细（PRD 完成标准）。
- 本功能点本期无独立详设定稿，**解决方案沿用通用批量审核规格（待 E-通用-B1 定稿）**；本节写清从 PRD/原型能拿到的权限范围/单条处理口径，不编造未定细节，标待确认指针。

### 开发类型：

- 新增节点。

### 节点目录：

- 服务商索赔管理 - 服务商索赔单列表（P01）- 批量审核。

### 业务流转说明：

- 区域负责人（对 `PENDING_REGION` 本区域单）或闪欣索赔专员（对 `PENDING_HQ` 全国单）在列表页勾选多条，触发批量审核。系统逐条按单条审核规则处理（区域初审=F-CLAIM-06 通过口径 / 业务审核=F-CLAIM-07 通过口径，含分流与下游生成），**每条独立事务，部分成功部分失败**，返回成功/失败明细（PRD S12「批量操作部分成功部分失败返回明细」）。**批量并发度、单条失败是否阻断、结果汇总结构等通用规格待 E-通用-B1 定稿**。

### 涉及数据表：

- `claim_order`（索赔主单）：逐条更新 `status` + 审计列。
- `claim_audit_record`（审核记录）：逐条写审核记录。
- `xw_claim_order` / `xw_claim_detail`（向欣旺达单，相邻链）：批量业务审核中 XW 单逐单触发生成（同 F-CLAIM-07）。

### 解决方案：

- **操作按钮**：
    - **批量审核**（列表页勾选多条）：
        - 权限范围：区域负责人只批本区域 `PENDING_REGION` 单；闪欣索赔专员批全国 `PENDING_HQ` 单（数据范围过滤在 SQL）
        - 单条处理口径：沿用对应单条审核规则——区域初审按 F-CLAIM-06 通过/驳回/拒绝口径（录 claim_target/project_code）、业务审核按 F-CLAIM-07 通过口径（XW/SELF 分流、XW 逐单生成向欣旺达单）
        - 结果：**部分成功部分失败**，逐条独立事务，返回成功/失败明细（成功条数、失败条数及原因）
        - **批量勾选上限、并发度、失败重试、结果汇总信封结构等通用规格待 E-通用-B1 定稿**
- 说明：批量审核录入项（如 claim_target/project_code/驳回原因）在通用批量规格中如何承载待 E-通用-B1 明确，本期不编造弹窗字段细节。

### 编码规则：

- 无（沿用各单据既有编码）。

### 外部接口：

- 无（XW 单生成属相邻链模块内调用，同 F-CLAIM-07）。

### 导入：

- 无。

### 防重复与并发：

- 每条单独走乐观锁 + 前置状态白名单（`PENDING_REGION`/`PENDING_HQ`），单条并发冲突只失败该条、不影响其它条。
- 核心动作叠加按 `claim_order.id` 分布式锁串行化，避免同单被单审与批量并发双审。
- **批量整体并发度/幂等键等通用规格待 E-通用-B1 定稿**。

---

## 留白与取数边界（本链）

- 工单库全部取数字段（工单状态/保内标识/业务类型/维修项目工时/备件明细/主责件/系数/VIN·SN·条码/维修方案技术审核结果/备件销售价）本期 **Mock/待挂载**，物理表列未登记，禁止自造；真库到位走变更登记回填（A-21）。
- 维修一级/二级分类等分类信息已按**裁决 D-索赔-B9** 落为本链真列 `claim_order.repair_type/sub_repair_type/serial_no/vin/province_city`（与 xw_claim_order 同名列对齐，值仍由工单库 Mock 带出）；F-CLAIM-02 A-39 条件必填场景触发与 F-CLAIM-06 A-14 汽车电子把关据此做**硬校验**。该 5 列为建表变更，schema/DDL 已登记并标「★ 列变更待研发组长审定后由人执行」。
- `claim_audit_record.audit_stage` 已按**裁决 D-索赔-B10** 增「责任判定」阶段码 `LIABILITY`，F-CLAIM-08 判责留痕用此值；schema/DDL 注释已同步。
- 备件销售价已按**裁决 D-索赔-B11** 落为 `claim_fee_part.sale_price` 真列（工单 Mock 带出），`mgmt_fee = sale_price × coefficient_snapshot` 事后可复核；该列变更 schema/DDL 已登记并标「★ 列变更待研发组长审定后由人执行」。
- 回运单实体/在途状态与 OA 内部转嫁契约仍**待挂载**；本期分别以回运附件（B-3）和仅记录比例/说明（B-6）作为 Mock/临时边界。备件「是否需回运」暂按 `part_attr` 文本判定（B-2）。
- QMS 预判值本期写审核备注（D-索赔-B4）；供应商责任本期仅置启动标志（D-索赔-B5）；均已在决策表确认。
- F-CLAIM-09 通用列表查询/导出规格（导出字段全集/异步策略）待 **E-通用-B2** 定稿；F-CLAIM-10 通用批量审核规格（并发度/失败处置/结果信封）待 **E-通用-B1** 定稿。
- 单据序列表 `doc_no_sequence` DDL 仍待研发组长审定（D-编码-B1）。
- 本链 D4 已决口径与仍挂起的待文档/待契约项集中登记在配套溯源表 C 表及 `docs/decisions/待确认决策表.xlsx`；待确认与产品决策落 `.sdd/decisions-DE.json`（stage=D）派生进决策表，本文正文非 PRD 直接事实处以 `（裁决 D-x）`/`（待确认 X-x/B-x/G-x）` 标注对应编号。
