# 接口文档 · 服务商索赔单链

> 通用说明（认证 / 基础 URL / `Result<T>` / `PageResult<T>` / 审计列 / 通用错误码 / 编号规则）见 `docs/conventions.md`，本文件不重复。
> 上游权威：`详细设计-服务商索赔单.md`（F-CLAIM-01~08）、`docs/db-schema/表结构文档.md`（claim_order / claim_fee_labor / claim_fee_part / claim_fee_other / claim_audit_record / claim_deduct_order / attachment）。每接口 = LLD 一个操作，每字段 = 已登记真列或 PRD 字段。
> 范围：只覆盖服务商索赔单从新建到进入待结算的完整链。工单库取数一律 Mock/待挂载；向欣旺达索赔单、抵扣单被结算/质保金消费、供应商索赔、OA 转嫁属相邻链，本文只写触发点与关联键，不写其内部字段。
> 乐观锁由后端管理，`version` 不在任何请求/响应体出现（conventions S2/S5）。主单更新走 status 双条件 `WHERE id=? AND status IN(<允许前置态>) AND deleted_at IS NULL`，MyBatis-Plus `@Version` 在事务内自读当前版本并自增；命中 0 行即并发/状态已变，回滚返回 `CLAIM_4008`。并发冲突前端靠 `status` + 冲突错误码（`CLAIM_4008`）感知，不传 `version`。

---

## 1. 接口列表

| 序 | 接口 | 方法 | 路径 | 对应 LLD 操作 / 功能编号 |
| :-- | :-- | :-- | :-- | :-- |
| 1 | 新建暂存草稿 | POST | `/claim/order/create-from-work-order` | 新建暂存草稿（F-CLAIM-01） |
| 2 | 编辑（仅其它费用） | PUT | `/claim/order/{id}` | 编辑（F-CLAIM-03） |
| 3 | 提交 / 重新提交 | POST | `/claim/order/{id}/submit` | 提交（F-CLAIM-02，含已驳回重提） |
| 4 | 作废 | POST | `/claim/order/{id}/void` | 作废（F-CLAIM-04） |
| 5 | 区域初审 | POST | `/claim/order/{id}/region-review` | 区域初审（F-CLAIM-06） |
| 6 | 业务审核 | POST | `/claim/order/{id}/business-audit` | 业务审核（F-CLAIM-07） |
| 7 | 判责录入 | POST | `/claim/order/{id}/liability` | 判责录入（F-CLAIM-08） |
| 8 | 详情 | GET | `/claim/order/{id}` | 查看（F-CLAIM-05） |
| 9 | 分页列表 | GET | `/claim/order/page` | 查看（F-CLAIM-05 列表） |

---

## 2. 接口详情

### 2.1 新建暂存草稿

- **接口名称**：新建暂存草稿
- **请求路径**：`/claim/order/create-from-work-order`
- **请求方式**：`POST`
- **描述**：服务商售后专员在「新建索赔单」入口，按已完工保内工单带出信息生成一张索赔单草稿（LLD 第二节 / F-CLAIM-01）。

#### 请求参数（Body - JSON）

| 参数名 | 类型 | 必选 | 说明 | 示例 |
| :--- | :--- | :--- | :--- | :--- |
| `workOrderId` | String | 是 | 源维修工单 id（落 `claim_order.work_order_id`，取数走工单库 Mock/待挂载） | `WO20260712000001` |
| `attachmentIds` | String[] | 否 | 草稿阶段可选上传的索赔单主体附件 id 列表（`biz_type=CLAIM`，空则不插；提交时再严格校验维修凭证） | `["1850000000000000001"]` |

> 主单业务字段（工单号/服务商/区域/系数/工时·备件·其它三类明细/主责件）由工单库 Mock 带出，非前端传参；`claim_no`/`status`/`currency`/审计列由后端生成。

#### 请求示例

```json
{
  "workOrderId": "WO20260712000001",
  "attachmentIds": ["1850000000000000001"]
}
```

#### 业务处理逻辑（写操作）

1. **前置校验**：
   - RBAC：登录角色为服务商售后专员，否则 `403`；数据范围按 `provider_id` 过滤（只动本服务商单）。
   - 工单准入（**工单库 Mock/待挂载**）：关联工单须「本服务商 + 状态=已完工 + **维修方案已过技术审核** + 保内」，任一不满足 → `CLAIM_4001`（技术审核前置来源 PRD F-CLAIM-01，本期由工单侧 Mock 保证）。
   - 工单业务类型映射：按工单 `work_order_biz_type` 查 `claim_type_mapping`（`enabled=1`）得 `claim_type`；查不到 → `CLAIM_4005`（不可静默）。
   - 条件唯一：同一 `work_order_id` 已存在状态非 `DECLINED/VOIDED` 的 `claim_order` → 拦下（`REJECTED` 亦占用，不能另建）。为防并发穿透，先以同一 DB 连接取工单维度互斥锁 `SELECT GET_LOCK(CONCAT('claim-wo-',MD5(workOrderId)),5)`，取锁失败/超时 → `CLAIM_4003`；取锁后事务内再查唯一性，`finally` 用同连接 `RELEASE_LOCK`（连接级锁，取放锁间不得归还连接池）。
   - 金额硬校验（从宽）：工时费合计 `> 0`（`Σ claim_fee_labor.amount`），否则 `CLAIM_4004`；其它费用合计 `≥ 0`。草稿阶段允许 `claim_target` 等必填项暂缺。
2. **落库**（同一事务）：
   - 取号：按 conventions S7 从 `doc_no_sequence`（前缀 `SPCO`）同事务行锁取 `claim_no`。
   - `INSERT claim_order`（一行）：`id`=雪花；`claim_no`=SPCO 号；`status`='DRAFT'；`currency`='CNY'；`work_order_id`/`work_order_no`/`work_order_biz_type`/`provider_id`/`provider_code`/`provider_name`/`region`=工单库带出（**Mock/待挂载**）；`claim_type`=映射结果（只读带入）；`coefficient_snapshot`=发起时点系数（优先工单库 Mock，无则查 `claim_coefficient` 按 `provider_id` 命中否则全局默认行的当前生效 `coefficient`）；`main_part_id`/`main_part_name`=工单主责件带出（**Mock/待挂载**，可空）；`claim_target`=NULL、`project_code`=NULL、`liability_result`/`liability_desc`/`liability_ratio`/`liability_by`/`liability_time`=NULL、`supplier_claim_started`=0、`provider_deduct_started`=0；`labor_fee_total`/`part_mgmt_fee`/`other_fee_total`=三张明细汇总回填，`apply_total_amount`=三者之和；`create_by`=当前用户、`created_at`=NOW()、`version`=0。
   - `INSERT claim_fee_labor`（按工单维修项目逐行）：`claim_id`=主单 id、`project_name`/`labor_hours`/`labor_price`=工单带出（**Mock/待挂载**）、`amount`=`labor_hours × labor_price`。
   - `INSERT claim_fee_part`（按工单备件逐行）：`claim_id`、`part_name`/`part_attr`/`battery_sn`/`old_part_code`=工单带出（**Mock/待挂载**）、`claim_amount`=0.00（恒 0）、`mgmt_fee`=`销售价 × coefficient_snapshot`。
   - `INSERT claim_fee_other`（按工单其它费用行）：`claim_id`、`fee_type`/`amount`/`remark`；`editable`=物流费等可人工改置 1，其余置 0（A-7）。
   - 附件（若 `attachmentIds` 非空）：逐个 `INSERT attachment`（`biz_type`='CLAIM'、`biz_id`=新单 id、`upload_by`=当前用户、`upload_time`=NOW()）。
   - `INSERT claim_audit_record`：`biz_type`='CLAIM'、`biz_id`=新单 id、`audit_stage`='OPERATION'、`audit_result`='CREATE'、`auditor`=当前用户、`audit_time`=NOW()、`remark`='新建草稿'。
3. **状态流转**：无 → `DRAFT`。
4. **副作用**：无下游触发。
5. **事务边界**：取号 + 主单 + 三张明细 + 附件 + 操作事件同一事务，任一失败整体回滚（回滚退号、不留半张单）；`RELEASE_LOCK` 在 `finally` 执行。

#### 响应参数

| 参数名 | 类型 | 说明 | 示例 |
| :--- | :--- | :--- | :--- |
| `success` | Boolean | 业务是否成功 | `true` |
| `code` | String | 业务码 | `"0"` |
| `message` | String | 提示信息 | `操作成功` |
| `data` | JSON | 业务数据 | |
| ├ `id` | String | 新索赔单 id | `1860000000000000001` |
| ├ `claimNo` | String | 索赔单编号 | `SPCO-20260712-0007` |
| ├ `status` | String | 单据状态 | `DRAFT` |
| ├ `applyTotalAmount` | BigDecimal | 申请总金额（工时+备件管理+其它，自动） | `3860.00` |
| `traceId` | String | 链路追踪 ID | `a1b2c3d4e5f6` |

#### 响应示例（成功）

```json
{
  "success": true,
  "code": "0",
  "message": "操作成功",
  "data": { "id": "1860000000000000001", "claimNo": "SPCO-20260712-0007", "status": "DRAFT", "applyTotalAmount": 3860.00 },
  "traceId": "a1b2c3d4e5f6"
}
```

---

### 2.2 编辑（仅其它费用）

- **接口名称**：编辑（仅其它费用）
- **请求路径**：`/claim/order/{id}`
- **请求方式**：`PUT`
- **描述**：服务商售后专员在草稿 / 已驳回态编辑「其它费用明细」（仅可编辑行），系统重算金额汇总（LLD 第四节 / F-CLAIM-03）。

#### 请求参数（Path + Body - JSON）

| 参数名 | 类型 | 必选 | 说明 | 示例 |
| :--- | :--- | :--- | :--- | :--- |
| `id` | String | 是 | 索赔单 id（Path） | `1860000000000000001` |
| `otherFees` | JSON[] | 是 | 其它费用明细编辑结果全集（仅 `editable=1` 行；未出现的既有 `editable=1` 行视为删除；`editable=0` 行禁止改删，若出现则拦截） | |
| ├ `id` | String | 否 | 明细行 id；有则更新，空则新增行 | `1861000000000000005` |
| ├ `feeType` | String | 是 | 费用类型（`claim_fee_other.fee_type`，如物流费/托车费，≤32） | `物流费` |
| ├ `amount` | BigDecimal | 是 | 金额（`claim_fee_other.amount`，≥0） | `140.00` |
| ├ `remark` | String | 否 | 备注（≤255，空写 NULL） | `顺丰到付` |

> 工时费明细、备件明细、备件管理费一律不可改（S8.2 固定，来源工单+系数），不在本请求体。

#### 请求示例

```json
{
  "otherFees": [
    { "id": "1861000000000000005", "feeType": "物流费", "amount": 140.00, "remark": "顺丰到付" },
    { "feeType": "托车费", "amount": 200.00 }
  ]
}
```

#### 业务处理逻辑（写操作）

1. **前置校验**：
   - RBAC：服务商售后专员 + 本服务商数据范围，否则 `403`。
   - 状态：前置 `DRAFT` 或 `REJECTED`（status 双条件 `WHERE id=? AND status IN('DRAFT','REJECTED')`，`@Version` 事务内自读版本自增，命中 0 行 → `CLAIM_4008`）。
   - 明细校验：每条 `feeType` 非空、`amount≥0`；请求内所有目标行必须为 `editable=1`，若命中 `editable=0` 行的改/删 → `400`。
2. **落库**（同一事务）：
   - `claim_fee_other`：按 `otherFees` upsert（带 `id` 更新 `fee_type`/`amount`/`remark`；无 `id` 新增，`editable`=1）；既有 `editable=1` 行未在集合中出现的删除。`editable=0` 行不动。
   - `UPDATE claim_order`：重算 `other_fee_total`=`Σ claim_fee_other.amount`，随后 `apply_total_amount`=`labor_fee_total + part_mgmt_fee + other_fee_total`（labor/part 两项不变）；`update_by`=当前用户、`updated_at`=NOW()、`version`=version+1；`status` 不变。
   - `INSERT claim_audit_record`：`audit_stage`='OPERATION'、`audit_result`='EDIT'、`auditor`/`audit_time`=操作人/NOW()、`remark`=本次其它费用增删改摘要。
3. **状态流转**：不变（`DRAFT` 仍 `DRAFT`、`REJECTED` 仍 `REJECTED`）。
4. **副作用**：无下游触发。
5. **事务边界**：明细 upsert/删除 + 主单汇总回写 + 操作事件同一事务；金额必须一致回写，任一失败整体回滚。

#### 响应参数

| 参数名 | 类型 | 说明 | 示例 |
| :--- | :--- | :--- | :--- |
| `success` | Boolean | 业务是否成功 | `true` |
| `code` | String | 业务码 | `"0"` |
| `message` | String | 提示信息 | `操作成功` |
| `data` | JSON | 业务数据 | |
| ├ `id` | String | 索赔单 id | `1860000000000000001` |
| ├ `otherFeeTotal` | BigDecimal | 重算后其它费用合计 | `340.00` |
| ├ `applyTotalAmount` | BigDecimal | 重算后申请总金额 | `4060.00` |
| `traceId` | String | 链路追踪 ID | `a1b2c3d4e5f6` |

#### 响应示例（成功）

```json
{ "success": true, "code": "0", "message": "操作成功", "data": { "id": "1860000000000000001", "otherFeeTotal": 340.00, "applyTotalAmount": 4060.00 }, "traceId": "a1b2c3d4e5f6" }
```

---

### 2.3 提交 / 重新提交

- **接口名称**：提交 / 重新提交
- **请求路径**：`/claim/order/{id}/submit`
- **请求方式**：`POST`
- **描述**：服务商售后专员提交草稿或重提已驳回单，通过硬校验后进入待区域初审（LLD 第三节 / F-CLAIM-02，已驳回重提同路径）。

#### 请求参数（Path + Body - JSON）

| 参数名 | 类型 | 必选 | 说明 | 示例 |
| :--- | :--- | :--- | :--- | :--- |
| `id` | String | 是 | 索赔单 id（Path） | `1860000000000000001` |

#### 请求示例

```json
{}
```

> 无请求体参数（幂等操作仅凭 Path `id` + 后端状态前置态判定）；示例见路径 `POST /claim/order/{id}/submit`。

#### 业务处理逻辑（写操作）

1. **前置校验**：
   - RBAC：服务商售后专员 + 本服务商数据范围，否则 `403`。
   - 状态：前置 `DRAFT` 或 `REJECTED`（乐观锁双条件，命中 0 行 → `CLAIM_4008`）。
   - 硬校验（同新建）：工单仍「已完工 + **维修方案已过技术审核** + 保内 + 本服务商」（**工单库 Mock/待挂载**，技术审核前置来源 PRD F-CLAIM-01）→ 不满足 `CLAIM_4001`；条件唯一仍成立；工时费合计 `>0` → 否则 `CLAIM_4004`。
   - 旧件回运（A-11）：逐条看 `claim_fee_part`「是否需回运」——按 `part_attr` 文本是否含「需回运」标识判定（**B-2：当前无结构化布尔位，暂按属性文本判定，待工单取数补布尔字段**）；需回运者须「已发起回运」——按「该单存在回运单附件」判定（**B-3：回运单实体/在途状态待挂载，附件为临时口径**）；存在需回运未发起 → `CLAIM_4006`。
   - 维修凭证附件必填（有据·硬校验，原始 PRD S4.1.1）：`attachment`（`biz_type='CLAIM'`）中至少 1 张维修凭证类附件，缺失 → `CLAIM_4007`；旧件资料附件仍选填，不随此项强制。
   - A-39 条件必填 + 位长校验（PRD S8.6/A-39）：`claim_fee_part.battery_sn`（电池包 SN）电池包维修场景必填且须 24 位；`claim_fee_part.old_part_code`（汽车电子旧件条码）汽车电子场景必填；整车 VIN 17 位落相邻链 `xw_claim_order`（本链 `claim_order`/明细无 VIN 列），本链豁免不校验。位长/条件必填不满足 → `CLAIM_4011`。**触发口径本期边界**：场景触发（电池包/汽车电子）依赖维修一级/二级分类，本链 `claim_order` 无 `repair_type/sub_repair_type` 列承接（schema 缺口，待确认），故本期分类判定依赖工单 Mock 源，与向欣旺达链 D-向欣-B3 同口径——Mock 已带出值时做位长校验，场景条件必填因分类字段未落库暂由工单 Mock 保证/不硬卡，真库接上后改硬校验。
2. **落库**（同一事务，乐观锁）：
   - `UPDATE claim_order`：`status`='PENDING_REGION'、`update_by`=当前用户、`updated_at`=NOW()、`version`=version+1；其余业务字段不动。
   - `INSERT claim_audit_record`：`audit_stage`='OPERATION'、`audit_result`= 由 `DRAFT` 提交为 'SUBMIT'、由 `REJECTED` 重提为 'RESUBMIT'、`auditor`/`audit_time`=操作人/NOW()、`remark`=操作摘要。
3. **状态流转**：`DRAFT` / `REJECTED` → `PENDING_REGION`（重提从头再走区域初审）。
4. **副作用**：无（等区域初审）。
5. **事务边界**：主单更新 + 操作事件同一事务；乐观锁未命中即回滚报冲突。

#### 响应参数

| 参数名 | 类型 | 说明 | 示例 |
| :--- | :--- | :--- | :--- |
| `success` | Boolean | 业务是否成功 | `true` |
| `code` | String | 业务码 | `"0"` |
| `message` | String | 提示信息 | `提交成功` |
| `data` | JSON | 业务数据 | |
| ├ `id` | String | 索赔单 id | `1860000000000000001` |
| ├ `status` | String | 单据状态 | `PENDING_REGION` |
| `traceId` | String | 链路追踪 ID | `a1b2c3d4e5f6` |

#### 响应示例（成功）

```json
{ "success": true, "code": "0", "message": "提交成功", "data": { "id": "1860000000000000001", "status": "PENDING_REGION" }, "traceId": "a1b2c3d4e5f6" }
```

---

### 2.4 作废

- **接口名称**：作废
- **请求路径**：`/claim/order/{id}/void`
- **请求方式**：`POST`
- **描述**：服务商售后专员作废草稿态索赔单（仅草稿可作废，A-15）（LLD 第五节 / F-CLAIM-04）。

#### 请求参数（Path + Body - JSON）

| 参数名 | 类型 | 必选 | 说明 | 示例 |
| :--- | :--- | :--- | :--- | :--- |
| `id` | String | 是 | 索赔单 id（Path） | `1860000000000000001` |
| `remark` | String | 否 | 作废原因（写 `claim_audit_record.remark`，≤500，空则记「作废」） | `工单信息有误，重新发起` |

#### 请求示例

```json
{ "remark": "工单信息有误，重新发起" }
```

#### 业务处理逻辑（写操作）

1. **前置校验**：
   - RBAC：服务商售后专员 + 本服务商数据范围，否则 `403`。
   - 状态：前置**仅** `DRAFT`（status 双条件 `WHERE id=? AND status='DRAFT'`，`@Version` 事务内自读版本自增，命中 0 行（非草稿/并发）→ `CLAIM_4008`）。
2. **落库**（同一事务）：
   - `UPDATE claim_order`：`status`='VOIDED'、`update_by`=当前用户、`updated_at`=NOW()、`version`=version+1；`deleted_at` 保持 NULL（已作废是可查看业务终态，不做技术软删）。
   - 明细行 `claim_fee_labor`/`claim_fee_part`/`claim_fee_other` 与 `attachment` 保留不删（供全状态查看与留痕）。
   - `INSERT claim_audit_record`：`audit_stage`='OPERATION'、`audit_result`='VOID'、`auditor`/`audit_time`=操作人/NOW()、`remark`=作废原因或「作废」。
3. **状态流转**：`DRAFT` → `VOIDED`（终态）。作废后该工单可重新发起（条件唯一放行 `VOIDED`，A-15）。
4. **副作用**：无。
5. **事务边界**：主单状态更新 + 操作事件同一事务（明细/附件不动）；乐观锁未命中即回滚。

#### 响应参数

| 参数名 | 类型 | 说明 | 示例 |
| :--- | :--- | :--- | :--- |
| `success` | Boolean | 业务是否成功 | `true` |
| `code` | String | 业务码 | `"0"` |
| `message` | String | 提示信息 | `已作废` |
| `data` | JSON | 业务数据 | |
| ├ `id` | String | 索赔单 id | `1860000000000000001` |
| ├ `status` | String | 单据状态 | `VOIDED` |
| `traceId` | String | 链路追踪 ID | `a1b2c3d4e5f6` |

#### 响应示例（成功）

```json
{ "success": true, "code": "0", "message": "已作废", "data": { "id": "1860000000000000001", "status": "VOIDED" }, "traceId": "a1b2c3d4e5f6" }
```

---

### 2.5 区域初审

- **接口名称**：区域初审
- **请求路径**：`/claim/order/{id}/region-review`
- **请求方式**：`POST`
- **描述**：区域负责人核查工单真实性与资料完整性，录主责件归属并作出通过 / 驳回 / 拒绝三选一（LLD 第六节 / F-CLAIM-06）。

#### 请求参数（Path + Body - JSON）

| 参数名 | 类型 | 必选 | 说明 | 示例 |
| :--- | :--- | :--- | :--- | :--- |
| `id` | String | 是 | 索赔单 id（Path） | `1860000000000000001` |
| `auditResult` | String | 是 | 审核结果，枚举 `通过`/`驳回`/`拒绝`（见附录 4.3） | `通过` |
| `claimTarget` | String | 条件 | 主责件归属 `XW`=欣旺达件 / `SELF`=自采件（`auditResult=通过` 时必填；驳回/拒绝可空） | `XW` |
| `projectCode` | String | 条件 | 项目编码（`claimTarget=XW` 时必填；`SELF` 置 NULL） | `PRJ-XW-2026-018` |
| `remark` | String | 条件 | 核查意见 / 驳回原因 / 拒绝原因（`驳回`/`拒绝` 时必填，≤500） | `工单资料齐全，予以通过` |
| `attachmentIds` | String[] | 否 | 本次审核附件 id 列表（写 `claim_audit_record.attachment_ids`，空写 NULL） | `["1850000000000000021"]` |

#### 请求示例

```json
{ "auditResult": "通过", "claimTarget": "XW", "projectCode": "PRJ-XW-2026-018", "remark": "工单资料齐全，予以通过" }
```

#### 业务处理逻辑（写操作）

1. **前置校验**：
   - RBAC：区域负责人，且数据范围只限本区域（SQL 拼 `region` 条件），否则 `403`。
   - 状态：前置 `PENDING_REGION`（乐观锁双条件，命中 0 行 → `CLAIM_4008`）。
   - 结果分支参数：`auditResult=通过` 时 `claimTarget` 必填、`claimTarget=XW` 时 `projectCode` 必填（缺失 → `CLAIM_4009`）；`驳回`/`拒绝` 时 `remark` 必填（缺失 → `400`）。
   - 汽车电子自由填报工时把关（A-14）：一级分类=汽车电子的单，区域负责人在此判工时合理性，不合理走「驳回」。
2. **电池包 SN 自动预判责任**（A-26，可覆盖·非终判）：对含 `battery_sn` 的备件，系统按 SN 预判责任倾向供质量部参考。预判规则来源 QMS，本期 **Mock/待挂载**；预判值无专用登记列，本期并入本次 `claim_audit_record.remark`（**D-索赔-B4：QMS 正式契约到位后再补预判字段**），不写 `claim_order.liability_result`（终判专用）。
3. **落库**（同一事务，乐观锁）——按 `auditResult` 分三支：
   - **通过**：`UPDATE claim_order` `status`='PENDING_HQ'、`claim_target`=录入值、`project_code`=录入值(XW) 或 NULL(SELF)、审计列刷新；`INSERT claim_audit_record`：`biz_type`='CLAIM'、`biz_id`=主单 id、`audit_stage`='区域初审'、`auditor`=当前用户、`audit_time`=NOW()、`audit_result`='通过'、`remark`=核查意见（含本期 Mock 的 SN 预判倾向）、`approved_amount`=NULL、`attachment_ids`=录入或 NULL。
   - **驳回**：`UPDATE claim_order` `status`='REJECTED'、审计列刷新（`claim_target`/`project_code` 已录可保留，重提会从头再录）；`INSERT claim_audit_record`：`audit_stage`='区域初审'、`audit_result`='驳回'、`remark`=驳回原因、`attachment_ids`=录入或 NULL。
   - **拒绝**：`UPDATE claim_order` `status`='DECLINED'、审计列刷新；`INSERT claim_audit_record`：`audit_stage`='区域初审'、`audit_result`='拒绝'、`remark`=拒绝原因。
4. **状态流转**：`PENDING_REGION` → `PENDING_HQ`（通过）/ `REJECTED`（驳回，可重提）/ `DECLINED`（拒绝，终态）。
5. **副作用**：无下游触发。
6. **事务边界**：主单状态 + 审核记录同一事务；乐观锁保并发。

#### 响应参数

| 参数名 | 类型 | 说明 | 示例 |
| :--- | :--- | :--- | :--- |
| `success` | Boolean | 业务是否成功 | `true` |
| `code` | String | 业务码 | `"0"` |
| `message` | String | 提示信息 | `区域初审已通过` |
| `data` | JSON | 业务数据 | |
| ├ `id` | String | 索赔单 id | `1860000000000000001` |
| ├ `status` | String | 单据状态 | `PENDING_HQ` |
| ├ `claimTarget` | String | 主责件归属（驳回/拒绝时可为 null） | `XW` |
| `traceId` | String | 链路追踪 ID | `a1b2c3d4e5f6` |

#### 响应示例（成功）

```json
{ "success": true, "code": "0", "message": "区域初审已通过", "data": { "id": "1860000000000000001", "status": "PENDING_HQ", "claimTarget": "XW" }, "traceId": "a1b2c3d4e5f6" }
```

---

### 2.6 业务审核

- **接口名称**：业务审核
- **请求路径**：`/claim/order/{id}/business-audit`
- **请求方式**：`POST`
- **描述**：闪欣索赔专员（全国）复核，通过后按主责件归属唯一分流——XW 进待结算并生成向欣旺达索赔单，SELF 进待责任判定（LLD 第七节 / F-CLAIM-07）。

#### 请求参数（Path + Body - JSON）

| 参数名 | 类型 | 必选 | 说明 | 示例 |
| :--- | :--- | :--- | :--- | :--- |
| `id` | String | 是 | 索赔单 id（Path） | `1860000000000000001` |
| `auditResult` | String | 是 | 审核结果，枚举 `通过`/`驳回`/`拒绝`（见附录 4.3） | `通过` |
| `remark` | String | 条件 | 审核意见 / 驳回原因 / 拒绝原因（`驳回`/`拒绝` 时必填，≤500） | `复核通过` |
| `attachmentIds` | String[] | 否 | 本次审核附件 id 列表（写 `claim_audit_record.attachment_ids`，空写 NULL） | `["1850000000000000031"]` |

> 分流依据 `claim_order.claim_target`（区域初审已录），非本次传参。

#### 请求示例

```json
{ "auditResult": "通过", "remark": "复核通过" }
```

#### 业务处理逻辑（写操作）

1. **前置校验**：
   - RBAC：闪欣索赔专员（全国数据范围），否则 `403`。
   - 状态：前置 `PENDING_HQ`（乐观锁双条件，命中 0 行 → `CLAIM_4008`）。
   - `驳回`/`拒绝` 时 `remark` 必填，缺失 → `400`。
2. **落库**（同一事务，乐观锁）——按 `auditResult` 与 `claim_target` 分支：
   - **通过 · claim_target=XW**：`UPDATE claim_order` `status`='PENDING_SETTLE'、审计列刷新；`INSERT claim_audit_record`：`audit_stage`='业务审核'、`audit_result`='通过'、`auditor`/`audit_time`/`remark`。**触发下游**：逐单自动生成一张向欣旺达索赔单（`xw_claim_order` + `xw_claim_detail` 草稿，金额=本单对应金额不乘系数 A-37；字段填充属向欣旺达链 LLD，本文只登记触发与关联键 `provider_claim_id=claim_order.id`、`provider_claim_no=claim_no`、编号前缀 `SXCO`）。生成与主单状态更新**同一事务**（审核通过即有下游单）；`claim_order` 侧无 xw 关联回写列。
   - **通过 · claim_target=SELF**：`UPDATE claim_order` `status`='PENDING_LIABILITY'、审计列刷新；`INSERT claim_audit_record`：`audit_stage`='业务审核'、`audit_result`='通过'。无自动生成，等质量部判责。
   - **驳回**：`UPDATE claim_order` `status`='REJECTED'、审计列刷新；`INSERT claim_audit_record`：`audit_stage`='业务审核'、`audit_result`='驳回'、`remark`=原因。可重提（回 `PENDING_REGION` 从头）。
   - **拒绝**：`UPDATE claim_order` `status`='DECLINED'、审计列刷新；`INSERT claim_audit_record`：`audit_stage`='业务审核'、`audit_result`='拒绝'、`remark`=原因。终态。
3. **状态流转**：`PENDING_HQ` → `PENDING_SETTLE`（XW 通过）/ `PENDING_LIABILITY`（SELF 通过）/ `REJECTED`（驳回）/ `DECLINED`（拒绝）。
4. **副作用**：XW 通过 → 生成向欣旺达单（本事务内，幂等靠下游 `xw_claim_detail.uk_provider_claim` 约束「一张服务商单仅纳入一张闪欣单」）；SELF/驳回/拒绝 无下游生成。
5. **事务边界**：主单 + 审核记录 +（XW 时）向欣旺达单生成，全在一个事务；向欣旺达单生成失败则整个审核回滚，主单不置通过（避免「通过了但没生成下游单」）。

#### 响应参数

| 参数名 | 类型 | 说明 | 示例 |
| :--- | :--- | :--- | :--- |
| `success` | Boolean | 业务是否成功 | `true` |
| `code` | String | 业务码 | `"0"` |
| `message` | String | 提示信息 | `业务审核已通过` |
| `data` | JSON | 业务数据 | |
| ├ `id` | String | 索赔单 id | `1860000000000000001` |
| ├ `status` | String | 单据状态 | `PENDING_SETTLE` |
| ├ `claimTarget` | String | 主责件归属（决定分流） | `XW` |
| ├ `xwClaimNo` | String | 生成的向欣旺达索赔单号（仅 XW 通过时返回，其余为 null） | `SXCO-20260712-0003` |
| `traceId` | String | 链路追踪 ID | `a1b2c3d4e5f6` |

#### 响应示例（成功）

```json
{ "success": true, "code": "0", "message": "业务审核已通过", "data": { "id": "1860000000000000001", "status": "PENDING_SETTLE", "claimTarget": "XW", "xwClaimNo": "SXCO-20260712-0003" }, "traceId": "a1b2c3d4e5f6" }
```

---

### 2.7 判责录入

- **接口名称**：判责录入
- **请求路径**：`/claim/order/{id}/liability`
- **请求方式**：`POST`
- **描述**：质量部责任判定专员对自采件单录责任判定结果，进入待结算；服务商责任自动生成索赔抵扣单（LLD 第八节 / F-CLAIM-08）。

#### 请求参数（Path + Body - JSON）

| 参数名 | 类型 | 必选 | 说明 | 示例 |
| :--- | :--- | :--- | :--- | :--- |
| `id` | String | 是 | 索赔单 id（Path） | `1860000000000000002` |
| `liabilityResult` | String | 是 | 责任判定结果 `SUPPLIER`/`PROVIDER`/`INTERNAL`（见附录 4.4） | `PROVIDER` |
| `liabilityDesc` | String | 是 | 责任说明（`claim_order.liability_desc`，≤200） | `服务商操作不当导致损坏` |
| `liabilityRatio` | BigDecimal | 条件 | 承担比例（%，`DECIMAL(5,2)`，`0<ratio≤100`；`PROVIDER` 必填，其余按需，D-索赔-B7） | `80.00` |
| `attachmentIds` | String[] | 否 | 判责附件 id 列表（`attachment`，`biz_type=LIABILITY`，空则不插） | `["1850000000000000041"]` |

#### 请求示例

```json
{ "liabilityResult": "PROVIDER", "liabilityDesc": "服务商操作不当导致损坏", "liabilityRatio": 80.00, "attachmentIds": ["1850000000000000041"] }
```

#### 业务处理逻辑（写操作）

1. **前置校验**：
   - RBAC：质量部责任判定专员，只限【待责任判定】的自采件单，否则 `403`。
   - 状态：前置 `PENDING_LIABILITY`（乐观锁双条件，命中 0 行 → `CLAIM_4008`）。
   - 字段：`liabilityDesc` 非空且 ≤200；`liabilityRatio` 满足 `0<ratio≤100`（不允许 0），`PROVIDER` 时必填——不满足 → `CLAIM_4010`。
2. **公共写入**（三种结果都写，同一事务，乐观锁）：
   - `UPDATE claim_order`：`liability_result`=录入值、`liability_desc`=录入值、`liability_ratio`=录入值、`liability_by`=当前用户、`liability_time`=NOW()、`status`='PENDING_SETTLE'、审计列刷新。
   - **判责操作留痕**（F-CLAIM-08 判责为独立用户动作，须与新建/提交/审核等一致进审核时间线，F-CLAIM-05 全流程留痕）：`INSERT claim_audit_record`：`biz_type`='CLAIM'、`biz_id`=主单 id、`audit_stage`=**责任判定**（⚠ schema `audit_stage` 枚举现无「责任判定」阶段码，属 schema 缺口，本期先登记留痕、阶段码待 schema 补齐后锁定，见待确认）、`audit_result`=判责结果 `SUPPLIER/PROVIDER/INTERNAL`（已在 `audit_result` 列注释登记）、`auditor`=判定人、`audit_time`=NOW()、`remark`=`liabilityDesc`、`attachment_ids`=判责附件 id（如有）。
   - 判责附件若有 → 逐条 `INSERT attachment`（`biz_type`='LIABILITY'、`biz_id`=主单 id、`upload_by`/`upload_time`）。
3. **按结果分别再做**：
   - **PROVIDER（服务商责任）→ 自动生成索赔抵扣单**：`UPDATE claim_order` `provider_deduct_started`=1；取号（前缀 `DKD`，conventions S7）后 `INSERT claim_deduct_order`：`id`=雪花、`deduct_no`=DKD 号、`claim_id`=主单 id、`claim_no`=主单号、`provider_id`=主单 provider_id、`work_order_no`=主单 work_order_no、`liability_result`='PROVIDER'、`liability_ratio`=录入比例、`claim_liable_amount`=`claim_order.apply_total_amount`（针对服务商全额应付，D-索赔-D1）、`deduct_amount`=`claim_liable_amount × liability_ratio / 100`（A-30）、`settle_period_belong`=该单 `claim_audit_record` 中「业务审核 / 通过」的 `DATE(audit_time)`（A-30，不得写 NULL）、`status`='UNSETTLED'。抵扣单被结算链消费冲减服务商应付，本链只负责生成。
   - **SUPPLIER（供应商责任）→ 触发供应商索赔**：`UPDATE claim_order` `supplier_claim_started`=1、标记**待挂载**；供应商索赔子流程为 V2 范围（PRD S5「9月项」），本期不生成实际单据或占位单（D-索赔-B5）。
   - **INTERNAL（内部责任）→ 记录比例**：仅公共写入的 `liability_result`/`liability_ratio`/`liability_desc`；费用内部转嫁交 OA，本期**待挂载**，不生成转嫁单（B-6，A-19）。
4. **状态流转**：三种结果统一 `PENDING_LIABILITY` → `PENDING_SETTLE`。
5. **副作用**：PROVIDER 生成抵扣单（本事务内，幂等靠 `deduct_no` 唯一 + 单向迁移天然防重）；SUPPLIER/INTERNAL 仅置标志/记录，实际下游待挂载。
6. **事务边界**：主单判责字段 + 判责留痕审核记录 + 附件 +（PROVIDER 时）抵扣单同一事务；抵扣单生成失败则判责整体回滚，主单不置待结算（避免「判了服务商责任却没抵扣单」）。

#### 响应参数

| 参数名 | 类型 | 说明 | 示例 |
| :--- | :--- | :--- | :--- |
| `success` | Boolean | 业务是否成功 | `true` |
| `code` | String | 业务码 | `"0"` |
| `message` | String | 提示信息 | `判责完成` |
| `data` | JSON | 业务数据 | |
| ├ `id` | String | 索赔单 id | `1860000000000000002` |
| ├ `status` | String | 单据状态 | `PENDING_SETTLE` |
| ├ `liabilityResult` | String | 责任判定结果 | `PROVIDER` |
| ├ `deductNo` | String | 生成的索赔抵扣单号（仅 PROVIDER 返回，其余为 null） | `DKD-20260712-0006` |
| ├ `deductAmount` | BigDecimal | 抵扣金额（仅 PROVIDER 返回，其余为 null） | `3088.00` |
| `traceId` | String | 链路追踪 ID | `a1b2c3d4e5f6` |

#### 响应示例（成功）

```json
{ "success": true, "code": "0", "message": "判责完成", "data": { "id": "1860000000000000002", "status": "PENDING_SETTLE", "liabilityResult": "PROVIDER", "deductNo": "DKD-20260712-0006", "deductAmount": 3088.00 }, "traceId": "a1b2c3d4e5f6" }
```

---

### 2.8 详情

- **接口名称**：索赔单详情
- **请求路径**：`/claim/order/{id}`
- **请求方式**：`GET`
- **描述**：查看索赔单主档 + 三张费用明细 + 审核 / 操作时间线 + 附件分组 + 判责字段 + 抵扣单（如有），任意状态可查（LLD 第九节 / F-CLAIM-05）。

#### 请求参数（Path）

| 参数名 | 类型 | 必选 | 说明 | 示例 |
| :--- | :--- | :--- | :--- | :--- |
| `id` | String | 是 | 索赔单 id（Path） | `1860000000000000001` |

#### 请求示例

```
GET /claim/order/1860000000000000001
```

#### 业务处理逻辑

无副作用，只读查询（均附 `deleted_at IS NULL`）：`claim_order` 主档 + `claim_fee_labor` / `claim_fee_part` / `claim_fee_other` 三张明细 + `claim_audit_record`（审核记录与 `audit_stage='OPERATION'` 操作事件，按 `audit_time,id` 升序，构成操作时间线）+ `attachment`（按 `biz_type` 分组：维修凭证/旧件/回运单归 `CLAIM`、判责归 `LIABILITY`）+ 判责字段 + `claim_deduct_order`（如有）；按角色数据范围（`provider_id`/`region`）过滤。无状态变更。

#### 响应参数

| 参数名 | 类型 | 说明 | 示例 |
| :--- | :--- | :--- | :--- |
| `success` | Boolean | 业务是否成功 | `true` |
| `code` | String | 业务码 | `"0"` |
| `message` | String | 提示信息 | `操作成功` |
| `data` | JSON | 索赔单详情 | |
| ├ `id` | String | 索赔单 id | `1860000000000000001` |
| ├ `claimNo` | String | 索赔单编号 | `SPCO-20260712-0007` |
| ├ `workOrderId` | String | 工单 id（Mock/待挂载） | `WO20260712000001` |
| ├ `workOrderNo` | String | 工单编号（待挂载） | `WO-20260712-001` |
| ├ `workOrderBizType` | String | 工单业务类型（待挂载） | `保内维修` |
| ├ `providerId` | String | 服务商 id（待挂载） | `PV0001` |
| ├ `providerCode` | String | 服务商编码 | `SP-SZ-001` |
| ├ `providerName` | String | 服务商名称 | `深圳某服务商有限公司` |
| ├ `region` | String | 所属区域 | `华东区/江苏` |
| ├ `claimType` | String | 索赔类型（工单映射带出只读） | `WARRANTY` |
| ├ `claimTarget` | String | 主责件归属（区域初审录，未录为 null） | `XW` |
| ├ `projectCode` | String | 项目编码（XW 必填） | `PRJ-XW-2026-018` |
| ├ `mainPartId` | String | 主责件 id（待挂载，可 null） | `PART0001` |
| ├ `mainPartName` | String | 主责件名称（待挂载，可 null） | `电池包总成` |
| ├ `coefficientSnapshot` | BigDecimal | 服务商系数快照（发起时点） | `1.2500` |
| ├ `laborFeeTotal` | BigDecimal | 工时费合计 | `600.00` |
| ├ `partMgmtFee` | BigDecimal | 备件管理费合计 | `3120.00` |
| ├ `otherFeeTotal` | BigDecimal | 其它费用合计 | `140.00` |
| ├ `applyTotalAmount` | BigDecimal | 申请总金额（自动） | `3860.00` |
| ├ `currency` | String | 币种 | `CNY` |
| ├ `status` | String | 单据状态 | `PENDING_SETTLE` |
| ├ `liabilityResult` | String | 责任判定结果（自采件判责后有值，否则 null） | `PROVIDER` |
| ├ `liabilityDesc` | String | 责任判定说明 | `服务商操作不当导致损坏` |
| ├ `liabilityRatio` | BigDecimal | 责任承担比例(%) | `80.00` |
| ├ `liabilityBy` | String | 判定人 | `王五` |
| ├ `liabilityTime` | String | 判定时间 | `2026-07-13 15:00:00` |
| ├ `supplierClaimStarted` | Integer | 是否已发起供应商索赔（0/1） | `0` |
| ├ `providerDeductStarted` | Integer | 是否已发起服务商抵扣（0/1） | `1` |
| ├ `laborFees` | JSON[] | 工时费明细 | |
| │  ├ `id` | String | 明细 id | `1861000000000000001` |
| │  ├ `projectName` | String | 维修项目名称 | `更换电池模组` |
| │  ├ `laborHours` | BigDecimal | 工时数 | `4.00` |
| │  ├ `laborPrice` | BigDecimal | 工时单价 | `150.00` |
| │  ├ `amount` | BigDecimal | 金额（工时数×单价） | `600.00` |
| ├ `partFees` | JSON[] | 备件明细 | |
| │  ├ `id` | String | 明细 id | `1862000000000000001` |
| │  ├ `partName` | String | 备件名称 | `电池模组` |
| │  ├ `partAttr` | String | 备件属性（需回运/SN 属性等） | `需回运` |
| │  ├ `claimAmount` | BigDecimal | 备件索赔金额（恒 0） | `0.00` |
| │  ├ `mgmtFee` | BigDecimal | 备件管理费（销售价×系数） | `3120.00` |
| │  ├ `batterySn` | String | 电池包 SN（24 位，可 null） | `SN0123456789ABCDEF012345` |
| │  ├ `oldPartCode` | String | 旧件条码（可 null） | `OLD-88001` |
| ├ `otherFees` | JSON[] | 其它费用明细 | |
| │  ├ `id` | String | 明细 id | `1863000000000000001` |
| │  ├ `feeType` | String | 费用类型 | `物流费` |
| │  ├ `amount` | BigDecimal | 金额 | `140.00` |
| │  ├ `remark` | String | 备注 | `顺丰到付` |
| │  ├ `editable` | Integer | 是否可编辑（0/1） | `1` |
| ├ `auditRecords` | JSON[] | 审核 / 操作时间线（按 audit_time,id 升序） | |
| │  ├ `id` | String | 记录 id | `1864000000000000001` |
| │  ├ `auditStage` | String | 阶段（OPERATION/区域初审/业务审核…） | `业务审核` |
| │  ├ `auditor` | String | 审核 / 操作人 | `李四` |
| │  ├ `auditTime` | String | 时间 | `2026-07-12 14:20:00` |
| │  ├ `auditResult` | String | 结果（审核 通过/驳回/拒绝；操作 CREATE/EDIT/SUBMIT/RESUBMIT/VOID） | `通过` |
| │  ├ `approvedAmount` | BigDecimal | 核准金额（部分通过，可 null） | `null` |
| │  ├ `remark` | String | 备注 / 原因 | `复核通过` |
| │  ├ `attachmentIds` | String | 附件 id 列表 | `null` |
| ├ `attachmentsByType` | JSON | 附件分组（key=biz_type：CLAIM/LIABILITY） | |
| │  ├ `bizType` | String | 附件业务类型 | `CLAIM` |
| │  ├ `items` | JSON[] | 该组附件 | |
| │  │  ├ `id` | String | 附件 id | `1850000000000000001` |
| │  │  ├ `fileName` | String | 文件名 | `维修照片.jpg` |
| │  │  ├ `filePath` | String | 相对路径 | `claim/2026/07/xxx.jpg` |
| │  │  ├ `fileType` | String | 文件类型（IMAGE/PDF） | `IMAGE` |
| ├ `deductOrder` | JSON | 索赔抵扣单（服务商责任生成，无则 null） | |
| │  ├ `id` | String | 抵扣单 id | `1870000000000000001` |
| │  ├ `deductNo` | String | 抵扣单号 | `DKD-20260712-0006` |
| │  ├ `liabilityResult` | String | 责任结果（=PROVIDER） | `PROVIDER` |
| │  ├ `liabilityRatio` | BigDecimal | 责任比例(%) | `80.00` |
| │  ├ `claimLiableAmount` | BigDecimal | 责任对应索赔金额 | `3860.00` |
| │  ├ `deductAmount` | BigDecimal | 抵扣金额 | `3088.00` |
| │  ├ `settlePeriodBelong` | String | 归属结算周期（按闪欣审批通过日期） | `2026-07-12` |
| │  ├ `status` | String | 抵扣单状态（UNSETTLED/SETTLED） | `UNSETTLED` |
| `traceId` | String | 链路追踪 ID | `a1b2c3d4e5f6` |

#### 响应示例（成功）

```json
{
  "success": true,
  "code": "0",
  "message": "操作成功",
  "data": {
    "id": "1860000000000000001",
    "claimNo": "SPCO-20260712-0007",
    "providerName": "深圳某服务商有限公司",
    "region": "华东区/江苏",
    "claimType": "WARRANTY",
    "claimTarget": "XW",
    "projectCode": "PRJ-XW-2026-018",
    "coefficientSnapshot": 1.2500,
    "laborFeeTotal": 600.00,
    "partMgmtFee": 3120.00,
    "otherFeeTotal": 140.00,
    "applyTotalAmount": 3860.00,
    "currency": "CNY",
    "status": "PENDING_SETTLE",
    "liabilityResult": null,
    "laborFees": [
      { "id": "1861000000000000001", "projectName": "更换电池模组", "laborHours": 4.00, "laborPrice": 150.00, "amount": 600.00 }
    ],
    "partFees": [
      { "id": "1862000000000000001", "partName": "电池模组", "partAttr": "需回运", "claimAmount": 0.00, "mgmtFee": 3120.00, "batterySn": "SN0123456789ABCDEF012345", "oldPartCode": "OLD-88001" }
    ],
    "otherFees": [
      { "id": "1863000000000000001", "feeType": "物流费", "amount": 140.00, "remark": "顺丰到付", "editable": 1 }
    ],
    "auditRecords": [
      { "id": "1864000000000000000", "auditStage": "OPERATION", "auditor": "张三", "auditTime": "2026-07-12 10:00:00", "auditResult": "CREATE", "approvedAmount": null, "remark": "新建草稿", "attachmentIds": null },
      { "id": "1864000000000000001", "auditStage": "业务审核", "auditor": "李四", "auditTime": "2026-07-12 14:20:00", "auditResult": "通过", "approvedAmount": null, "remark": "复核通过", "attachmentIds": null }
    ],
    "attachmentsByType": [
      { "bizType": "CLAIM", "items": [ { "id": "1850000000000000001", "fileName": "维修照片.jpg", "filePath": "claim/2026/07/xxx.jpg", "fileType": "IMAGE" } ] }
    ],
    "deductOrder": null
  },
  "traceId": "a1b2c3d4e5f6"
}
```

---

### 2.9 分页列表

- **接口名称**：索赔单分页列表
- **请求路径**：`/claim/order/page`
- **请求方式**：`GET`
- **描述**：多维筛选分页查询索赔单，按角色数据范围过滤（F-CLAIM-05 列表）。

#### 请求参数（Query）

| 参数名 | 类型 | 必选 | 说明 | 示例 |
| :--- | :--- | :--- | :--- | :--- |
| `claimNo` | String | 否 | 索赔单号（模糊） | `SPCO-20260712` |
| `workOrderNo` | String | 否 | 工单编号（模糊） | `WO-20260712` |
| `providerName` | String | 否 | 服务商名称（模糊） | `深圳` |
| `region` | String | 否 | 所属区域 | `华东区/江苏` |
| `claimType` | String | 否 | 索赔类型（`CLAIM_TYPE`，见附录 4.5） | `WARRANTY` |
| `claimTarget` | String | 否 | 主责件归属（`XW`/`SELF`） | `XW` |
| `status` | String | 否 | 单据状态（`CLAIM_STATUS`，见附录 4.2） | `PENDING_SETTLE` |
| `createdAtStart` | String | 否 | 创建时间起（按 `created_at`） | `2026-07-01` |
| `createdAtEnd` | String | 否 | 创建时间止 | `2026-07-31` |
| `pageNo` | Long | 是 | 页码（从 1 起） | `1` |
| `pageSize` | Long | 是 | 每页条数 | `20` |

#### 请求示例

```
GET /claim/order/page?status=PENDING_SETTLE&claimTarget=XW&pageNo=1&pageSize=20
```

#### 业务处理逻辑

无副作用，只读分页查询（附 `deleted_at IS NULL`）；按角色数据范围（`provider_id`/`region`）过滤；返回 `PageResult<索赔单列表行>`。

#### 响应参数

| 参数名 | 类型 | 说明 | 示例 |
| :--- | :--- | :--- | :--- |
| `success` | Boolean | 业务是否成功 | `true` |
| `code` | String | 业务码 | `"0"` |
| `message` | String | 提示信息 | `操作成功` |
| `data` | JSON | 分页数据 | |
| ├ `total` | Long | 总条数 | `42` |
| ├ `pageNo` | Long | 当前页码 | `1` |
| ├ `pageSize` | Long | 每页条数 | `20` |
| ├ `list` | JSON[] | 列表行 | |
| │  ├ `id` | String | 索赔单 id | `1860000000000000001` |
| │  ├ `claimNo` | String | 索赔单号 | `SPCO-20260712-0007` |
| │  ├ `workOrderNo` | String | 工单编号 | `WO-20260712-001` |
| │  ├ `providerName` | String | 服务商名称 | `深圳某服务商有限公司` |
| │  ├ `region` | String | 所属区域 | `华东区/江苏` |
| │  ├ `claimType` | String | 索赔类型 | `WARRANTY` |
| │  ├ `claimTarget` | String | 主责件归属 | `XW` |
| │  ├ `applyTotalAmount` | BigDecimal | 申请总金额 | `3860.00` |
| │  ├ `status` | String | 单据状态 | `PENDING_SETTLE` |
| │  ├ `createdAt` | String | 创建时间 | `2026-07-12 10:00:00` |
| `traceId` | String | 链路追踪 ID | `a1b2c3d4e5f6` |

#### 响应示例（成功）

```json
{
  "success": true,
  "code": "0",
  "message": "操作成功",
  "data": {
    "total": 42, "pageNo": 1, "pageSize": 20,
    "list": [
      { "id": "1860000000000000001", "claimNo": "SPCO-20260712-0007", "workOrderNo": "WO-20260712-001", "providerName": "深圳某服务商有限公司", "region": "华东区/江苏", "claimType": "WARRANTY", "claimTarget": "XW", "applyTotalAmount": 3860.00, "status": "PENDING_SETTLE", "createdAt": "2026-07-12 10:00:00" }
    ]
  },
  "traceId": "a1b2c3d4e5f6"
}
```

---

## 3. 错误码（本模块业务专属）

> 通用错误码见 `docs/conventions.md S6`（`400`/`401`/`403`/`404`/`409`/`500`）。

| 错误码 | 说明 | 触发场景 |
| :--- | :--- | :--- |
| `CLAIM_4001` | 工单不满足索赔准入 | 关联工单非「本服务商 + 已完工 + 维修方案已过技术审核 + 保内」（工单库 Mock 判定，A-8 / PRD F-CLAIM-01 技术审核前置） |
| `CLAIM_4002` | 该工单已存在有效索赔单 | 同 `work_order_id` 已有状态非 `DECLINED/VOIDED` 的索赔单（条件唯一，A-15） |
| `CLAIM_4003` | 该工单正在发起索赔，请稍后重试 | 工单维度互斥锁 `GET_LOCK` 取锁失败 / 超时（并发发起） |
| `CLAIM_4004` | 工时费合计必须大于 0 | 新建 / 提交时 `Σ claim_fee_labor.amount ≤ 0`（A-1） |
| `CLAIM_4005` | 工单业务类型未配置映射 | `claim_type_mapping` 查不到该 `work_order_biz_type`（A-9，不可静默） |
| `CLAIM_4006` | 存在需回运备件未发起回运 | 提交时需回运备件缺回运单附件（A-11，B-2/B-3 临时口径） |
| `CLAIM_4007` | 缺少维修凭证附件 | 提交时无维修凭证类附件（原始 PRD S4.1.1 硬校验） |
| `CLAIM_4008` | 当前状态不允许该操作，请刷新 | 乐观锁双条件命中 0 行（并发 version 变 / 状态不在前置白名单 / 终态流转，第十节非法迁移） |
| `CLAIM_4009` | 主责件归属或项目编码缺失 | 区域初审通过时 `claimTarget` 未填，或 `claimTarget=XW` 未填 `projectCode` |
| `CLAIM_4010` | 责任承担比例非法 | 判责 `liabilityRatio` 不满足 `0<ratio≤100`，或 `PROVIDER` 未填比例（D-索赔-B7） |
| `CLAIM_4011` | 条件必填/位长校验不通过 | 提交时电池包 SN 非 24 位、或电池包/汽车电子场景 SN/旧件条码缺失（PRD S8.6/A-39；场景触发依赖分类字段，本期 Mock 口径同 D-向欣-B3） |

---

## 4. 附录 · 枚举字典

> 请求 / 响应出现的枚举在此列全（值 + 含义），正文不再省略。

### 4.1 涉及表与关联键

| 表 | 说明 | 关联键 |
| :--- | :--- | :--- |
| `claim_order` | 索赔单主单 | `id`（雪花）/ `claim_no`（SPCO） |
| `claim_fee_labor` / `claim_fee_part` / `claim_fee_other` | 工时 / 备件 / 其它费用明细 | `claim_id → claim_order.id` |
| `claim_audit_record` | 审核记录与操作事件 | `biz_type='CLAIM'` + `biz_id=claim_order.id` |
| `claim_deduct_order` | 索赔抵扣单（服务商责任生成） | `claim_id`/`claim_no → claim_order` |
| `attachment` | 附件 | `biz_type`（CLAIM/LIABILITY）+ `biz_id=claim_order.id` |
| `xw_claim_order`（相邻链） | 向欣旺达索赔单（XW 通过触发生成） | `provider_claim_id`/`provider_claim_no → claim_order`（本文只登记触发与关联键，不写其字段） |

### 4.2 索赔单状态 `claim_order.status`（`CLAIM_STATUS`）

| 值 | 含义 |
| :--- | :--- |
| `DRAFT` | 草稿 |
| `PENDING_REGION` | 待区域初审 |
| `PENDING_HQ` | 待总部审核（业务审核） |
| `PENDING_LIABILITY` | 待责任判定 |
| `PENDING_SETTLE` | 待结算 |
| `REJECTED` | 已驳回（可编辑重提） |
| `DECLINED` | 已拒绝（终态） |
| `SETTLED` | 已结算（由结算链驱动，本链不主动写） |
| `VOIDED` | 已作废（终态） |

### 4.3 审核结果（`claim_audit_record.audit_result`，审核节点）

| 值 | 含义 |
| :--- | :--- |
| `通过` | 审核通过（区域初审 / 业务审核） |
| `驳回` | 退回服务商（→ REJECTED，可重提） |
| `拒绝` | 终止不予受理（→ DECLINED，终态） |

### 4.4 责任判定结果 `claim_order.liability_result`（`LIABILITY_RESULT`）

| 值 | 含义 | 后续 |
| :--- | :--- | :--- |
| `SUPPLIER` | 供应商责任 | 置 `supplier_claim_started=1`，供应商索赔 V2 待挂载（B-5） |
| `PROVIDER` | 服务商责任 | 置 `provider_deduct_started=1`，本事务生成索赔抵扣单 |
| `INTERNAL` | 内部责任 | 仅记录比例 / 说明，OA 内部转嫁待挂载（B-6） |

### 4.5 索赔类型 `claim_order.claim_type`（`CLAIM_TYPE`）

| 值 | 含义 |
| :--- | :--- |
| `WARRANTY` | 保内维修 |
| `SERVICE_ACTIVITY` | 服务活动 |
| `INTERNAL` | 内部服务 |
| `SPECIAL` | 特殊工单 |

### 4.6 主责件归属 `claim_order.claim_target`（`CLAIM_TARGET`）

| 值 | 含义 | 业务审核分流 |
| :--- | :--- | :--- |
| `XW` | 欣旺达件 | 通过 → PENDING_SETTLE + 生成向欣旺达索赔单 |
| `SELF` | 自采件 | 通过 → PENDING_LIABILITY（待质量部判责） |

### 4.7 操作事件结果（`claim_audit_record.audit_result`，`audit_stage='OPERATION'`）

| 值 | 含义 |
| :--- | :--- |
| `CREATE` | 新建草稿 |
| `EDIT` | 编辑（其它费用） |
| `SUBMIT` | 提交 |
| `RESUBMIT` | 已驳回重提 |
| `VOID` | 作废 |

### 4.8 抵扣单状态 `claim_deduct_order.status`（`DEDUCT_STATUS`）

| 值 | 含义 |
| :--- | :--- |
| `UNSETTLED` | 未结算 |
| `SETTLED` | 已纳入结算 |

### 4.9 附件业务类型 `attachment.biz_type`（本模块用到）

| 值 | 含义 |
| :--- | :--- |
| `CLAIM` | 索赔单主体附件（维修凭证 / 旧件资料 / 回运单，A-11 临时口径） |
| `LIABILITY` | 判责录入附件 |

### 4.10 本模块 Mock / 待挂载 / 待确认登记

| 项 | 处置 | 来源 |
| :--- | :--- | :--- |
| 工单取数（状态/保内/业务类型/维修项目工时/备件/主责件/系数/VIN·SN·条码） | Mock/待挂载，物理列未登记，禁自造 | LLD 十二节、表结构「既有维修工单库」 |
| SN 电池包责任预判 | QMS 规则 Mock/待挂载，预判值本期并入 `claim_audit_record.remark`，不新增专列 | A-26、D-索赔-B4 |
| 「是否需回运」判定 | 无结构化布尔位，暂按 `part_attr` 文本含「需回运」判定 | A-11、B-2 |
| 「已发起回运」判定 | 回运单实体 / 在途状态待挂载，暂以「存在回运单附件」为临时证据 | A-11、B-3 |
| 供应商责任下游 | V2 范围，仅置 `supplier_claim_started=1`，不生成单据 / 占位单 | PRD S5、D-索赔-B5 |
| 内部责任 OA 转嫁 | 待挂载，仅记录比例 / 说明，不生成转嫁单 | A-19、B-6 |
| 向欣旺达索赔单生成 | 触发点 + 关联键（`provider_claim_id`/`provider_claim_no`/前缀 SXCO）在本链登记，字段填充属向欣旺达链 LLD | A-36/A-37 |
| `doc_no_sequence` 建表 SQL | 待研发组长审定（D-编码-B1），审定执行前不得建表 | conventions S7 |
